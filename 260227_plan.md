# V5 모델 리디자인 — 260227 (구현 완료)

## 상태: ✅ 전체 구현 완료, Smoke test 통과

브랜치: `20260223_trial_debugged`
파라미터: **3,846,782 → 2,932,174 (23.8% 감소)**

---

## 문제 정의

현재 Transformer decoder 모델 (~3.85M params)을 ~7,800 CARLA 레이싱 시나리오로 학습 중. 구조적 문제:

1. **z_global collapse**: 토큰에 1회 주입 후 4 layer 통과 시 희석 → posterior가 prior로 수렴
2. **과도한 decoder capacity**: 4 layer x FFN 512 → decoder가 z_global 무시 가능
3. **A2A에 물리적 정보 없음**: Agent 간 attention이 순수 embedding 기반, 상대 물리량 미반영
4. **TF-AR gap**: 학습은 GT state, 추론은 예측 state → 분포 불일치

---

## 변경 사항 요약

| # | 변경 | 목적 | 파일 | 상태 |
|---|------|------|------|------|
| 1 | AdaLN | z_global 매 layer 재주입 | model.py | ✅ |
| 2 | Decoder 축소 | 4→2 layer, FFN 512→256 | cfg | ✅ |
| 3 | A2A Relative Bias | 8개 상대 물리량 → attention bias | model.py | ✅ |
| 4 | z_global Aux Decoder | z만으로 action 예측 → aux loss | model.py, loss.py | ✅ |
| 5 | Health Monitoring | z/intent/map/grad TensorBoard 로깅 | model.py, train.py | ✅ |
| 6 | Action Blending | TF→AR 전환: action 내분 + bicycle | model.py, train.py | ✅ |
| * | Step 기반 Annealing | map_attn, blend_alpha → step 기반 | train.py | ✅ |
| * | Config/Parser/학습 flow | 전체 연결 | train.py, cfg | ✅ |

모든 변경을 동시 구현. Action blending은 alpha=0(순수 TF)에서 시작하므로 단계 분리 불필요.

---

## 파라미터 상세

```
=== V5 Total: 2,932,174 ===
  Map CNN (early+late+feat):                      326,544
  Encoder (step_feat+gcn+gru+TF_enc+prior+post): 1,205,760
  Interaction GCN:                                 162,496
  Token proj + Temporal PE:                         15,104
  TransDecoder layers (2 layers):                1,135,440
  Intent (codebook+proj, ego+sur):                  60,027
  Output heads (ego+sur):                              516
  Aux heads (sur_pred+ego_pred+intent_ce):             813
  z_aux (embed+decoder):                            25,474

=== TransDecoder Layer 당 내역 (567,720 each) ===
  AdaLN norms (5개):    187,520  ← NEW (기존 LN은 256/each)
  A2T attn:              66,048
  A2A attn (SharedKV):   99,072
  A2A rel bias module:      552  ← NEW
  A2S map cross-attn:    83,188
  FFN ego+sur (256 dim): 131,840  ← REDUCED (기존 512 = 263,680)
```

---

## Change 1: AdaLN (Adaptive Layer Normalization)

### 설계
5개 LayerNorm을 AdaLN으로 교체: `y = gamma * LN(x) + beta`, `(gamma, beta) = MLP(z_global)`
z_global을 매 layer, 매 normalization 지점에서 재주입하여 희석 방지.

### 판단 근거
- DiT (Diffusion Transformer) 논문에서 검증된 방식
- z_global conditioning이 매 layer에서 active하게 유지됨
- Identity init (gamma=1, beta=0) → 초기에 기존 LN과 동일. 점진적으로 z 반영

### 구현 위치: `src/models/trafficplanner_model.py`

1. **`AdaLN` 클래스** (line 51-74)
   - `__init__(d_model=128, z_size=32)` → MLP: z(32)→128(SiLU)→256 → chunk → gamma(128), beta(128)
   - Identity init: 마지막 layer weight=0, bias=[1...1, 0...0]
   - `forward(x, z_cond)` → `gamma * LayerNorm(x) + beta`

2. **`TransDecoderLayer.__init__`** (line 207) — `use_adaln=False, z_size=32` 추가
   - 5개 norm 조건부 교체: `a2t_norm`, `a2a_norm`, `a2s_norm`, `ego_ffn_norm`, `sur_ffn_norm`

3. **`TransDecoderLayer.forward`** (line 334) — `z_global=None` 추가
   - z_global broadcasting:
     - A2T: `(B*N, T, D)` ← z unsqueeze(1) for T broadcast
     - A2A/A2S: `(B*T, N, D)` ← z unsqueeze(0) for BT broadcast
     - FFN: `(B, T, N_ego/sur, D)` ← z[idx] unsqueeze(0).unsqueeze(0)

4. **호출부**: `transformer_decoder_training`, `transformer_decoder_inference`, `transformer_decoder_training_blended` — 모두 `z_global=z if self.use_adaln else None` 전달

### 파라미터: ~370K 추가 (37K/norm × 5 norms × 2 layers)

---

## Change 2: Decoder 축소

### 설계
Transformer decoder 4→2 layer, FFN 512→256. 과도한 capacity 줄여 z_global 의존도 증가.

### 판단 근거
- FFN 256 = d_model(128)의 2× expansion → 일반적으로 충분. 4×(512)은 대규모 데이터셋용
- 7,800 시나리오에 2.1M decoder는 과다 → 1.1M으로 축소
- capacity 제한 → decoder가 z_global에 더 의존하게 강제
- Intent codebook은 Layer 0-1 사이이므로 2-layer에서도 유효

### 수정: `configs/train_trafficplanner.cfg`
```
trans_num_layers: 2    # was 4
trans_ffn_dim: 256     # was 512
```
코드 변경 없음 — cfg 값으로 동적 생성.

---

## Change 3: A2A Relative Physical Bias

### 설계
8개 pairwise 물리적 feature 계산 → MLP → softmax 이전 attention score에 additive bias.
zero-init이므로 초기에는 기존 attention과 동일, 학습 과정에서 물리적 관계 반영.

### 방식 선정 근거
- **Attention Bias** (scalar additive): ALiBi/T5에서 검증된 방식, ~552 파라미터
- **Token concat 아님**: self-pair에서 의미 없는 정보가 embedding 오염. Attention score 직접 조작이 "이 agent 쌍에 얼마나 주의할지"를 직접 제어
- Key 변조는 공유 K/V 구조 파괴, Post-softmax gating은 확률 합 파괴 → 불적합

### 8개 Feature (query agent i → key agent j)

| # | Feature | 설명 | 선택 근거 |
|---|---------|------|----------|
| 1 | rel_long | i heading 기준 종방향 상대거리 | 전방/후방 agent 구분 |
| 2 | rel_lat | i heading 기준 횡방향 상대거리 | 좌/우 차선 agent 구분 |
| 3 | dist | 유클리드 거리 | 절대 거리 |
| 4 | rel_v_long | 종방향 상대속도 | 추월/감속 패턴 |
| 5 | rel_v_lat | 횡방향 상대속도 | 차선 변경 감지 |
| 6 | closing_speed | 접근속도 = -(Δpos·Δvel)/dist | 1D 접근/이탈 요약 |
| 7 | ttc | Time-to-Collision | 안전 관련 핵심 지표 |
| 8 | rel_heading | atan2(cross, dot) | 동방향/역방향 주행 구분 |

### 구현 위치: `src/models/trafficplanner_model.py`

1. **`A2ARelativeBias` 클래스** (line 77-94): MLP 8→32(ReLU)→8heads, zero-init 출력
2. **`_compute_rel_features_static()` 정적 메서드** (line 425-465): states (BT,N,6) → features (BT,N,N,8)
   - normalized space에서 계산 (MLP가 스케일 학습)
   - heading은 (hx,hy) unit vector → 직접 회전 가능
3. **`SharedKVAttention._attention`** (line 128): `rel_bias=None` → `attn_scores += rel_bias`
4. **`SharedKVAttention.forward`** (line 152): `rel_bias=None`, ego/sur 행 슬라이싱
5. **`TransDecoderLayer`** (line 228-231): bias 모듈 생성, `agent_states` 받아서 계산
6. **호출부**: training에서 `all_states.permute(1,0,2)` 전달, inference에서 `accum_states` 추적

### 파라미터: 552 (무시 가능)

---

## Change 4: z_global Auxiliary Decoder

### 설계
z_global(32) + step_embed(32) → MLP(64→128→128→2) → action(acc, ddh) 예측.
학습 시에만 사용하는 auxiliary MSE loss. z_global이 trajectory 정보를 충분히 담도록 강제.

### 판단 근거

**왜 MLP인가 (Transformer가 아닌 이유)**:
- 목적은 좋은 trajectory가 아니라 z_global의 **정보량 probe**
- Transformer 쓰면 decoder 자체 capacity로 z_global 무시 가능 → 본말전도
- MLP만으로도 z_global에서 복원 가능하다면 = z_global이 정보를 잘 담고 있다는 증거

**왜 action space MSE인가 (position space가 아닌 이유)**:
1. bicycle model AR loop 불필요 → 연산 간단
2. AR 오차 누적 없음 → 각 step 독립적 gradient (후반 step에 편향 안 됨)
3. position space와 수학적 동치 (action 정확 → bicycle → position 정확)

**GT action 역산**: `acc = (speed_next - speed_cur) / dt`, `ddh = (hdot_next - hdot_cur) / dt`
- car_dynamics의 정확한 역연산. clamp 후 effective action 복원

**step_embed**: Positional encoding 역할. 없으면 12 step 모두 동일 출력. 12개 step 병렬 생성 (AR 없음)

**posterior에서만 의미**: z_global은 posterior(미래 GT 전체 관측 후 인코딩)에서 나옴 → 미래 상호작용 결과도 포함. 이 정보를 압축/암기하게 강제하는 것이 목적. inference 시 prior→z 근사는 KL loss가 담당

**weight 0.1**: recon_loss ~5 대비 z_aux 기여 ~0.1 수준. z_global에 gradient signal은 주되 학습을 지배하지 않음

### 구현 위치

**`src/models/trafficplanner_model.py`**
1. `__init__` (line 733-741): `z_aux_step_embed` Embedding(12, 32), `z_aux_decoder` MLP(64→128→128→2)
2. `forward()` (line 796-809): ego z(32) + step_embed(32) → MLP → (N_ego, FT, 2) → `self._z_aux_traj`
3. `_compute_gt_actions()` (line 1838-1886): GT states 역산 → normalized action

**`src/losses/trafficplanner_loss.py`**
4. `_compute_auxiliary_losses()`: `z_aux_loss = F.mse_loss(model._z_aux_traj, gt_actions)`, weight from `loss_weights['z_aux']`

### 파라미터: ~25K (무시 가능)

---

## Change 5: Health Monitoring

### 설계
학습 중 각 모듈의 건강 상태를 TensorBoard에서 실시간 확인.

### `compute_health_metrics()` — 모델 내부 상태

| Metric | 목적 |
|--------|------|
| `z_global/norm` | z sample L2 norm 평균 |
| `z_global/std` | z dim별 std → 평균 (0이면 collapse) |
| `z_global/min_dim_std` | 가장 죽은 dim 확인 |
| `z_global/active_dims` | std > 0.01인 dim 수 (32 중 몇 개 활성) |
| `z_global/kl_per_dim_mean` | per-dim KL 평균 |
| `z_global/kl_per_dim_max` | 가장 큰 KL dim |
| `z_global/prior_post_mean_gap` | posterior가 prior에서 이동량 (0이면 collapse) |
| `z_global/posterior_var_mean` | posterior variance |
| `intent/entropy` | intent weight entropy |
| `intent/max_prob` | 최대 intent 확률 평균 |
| `intent/unique_slots` | 사용된 unique intent 개수 |
| `intent/slot_k_frac` | slot별 사용 빈도 (mode collapse 감지) |
| `map_attn/entropy` | map attention entropy |
| `map_attn/peak` | map attention 최대값 평균 |
| `map_attn/top5_mass` | top-5 토큰 attention 집중도 |
| `z_aux/pred_acc_std`, `pred_ddh_std` | z_aux decoder 출력 다양성 |
| `adaln/L{i}_{norm}_wnorm` | AdaLN weight norm (0이면 identity, 커지면 z 활용 중) |

### `compute_per_module_grad_norms()` — 모듈별 gradient

encoder_gcn, prior, posterior, interaction_gcn, trans_layers, output_heads, intent, map_cnn, z_aux 각각의 gradient L2 norm.

### Logging 빈도
- 매 50 step: total + per-module grad norms
- 매 500 step: health metrics (`health_step/`)
- 매 epoch: health metrics (`health/`)

### 구현 위치
- `src/models/trafficplanner_model.py`: `compute_health_metrics()` (line 831-900), `compute_per_module_grad_norms()` (line 902-930)
- `src/train_trafficplanner.py`: grad norm logging (line 320-331), health per 500 step (line 400-403), health per epoch (line 857-859)

---

## Change 6: Action Blending

### 설계
```
a_applied = (1-α)·a_GT + α·â_predicted
s_{t+1} = BicycleModel(s_t, a_applied)
α: 0 → target (step 기반 linear annealing)
```
α>0이면 AR loop 필수. α=0에서 시작하므로 초기에는 순수 TF.

### 판단 근거
- Scheduled Sampling의 action-level 변형
- State-level mixing은 bicycle model consistency 깨뜨림
- α=0에서 시작 → 초기 학습은 기존 TF와 완전 동일, 안정적
- AR loop이지만 gradient 유지 (no detach) → end-to-end 학습

### 핵심 동작 (blended mode)
```
Step 0: past(4 GT tokens, z_global 포함) → Transformer(AdaLN, A2A bias) → output head → pred_action
        blended = (1-α)*GT_action + α*pred_action
        bicycle_model(GT_last_past, blended) → new_state  ← GT가 아님!
        new_state → GCN → token_proj(gcn + z_global + lw + sem) → new_token → append

Step 1: past(4 GT) + step0(blended state) → Transformer → ...
        blended = (1-α)*GT_action + α*pred_action
        bicycle_model(step0의 blended_state, blended) → new_state
        ...
```

- z_global은 3경로로 계속 주입: (1) token_proj concat, (2) AdaLN 매 layer, (3) temporal PE 독립
- 매 step의 **state는 GT가 아님** — blend된 action으로 bicycle model을 돌린 결과
- GT는 **action 수준**에서만 (1-α) 비율로 혼합

### 구현 위치

**`configs/train_trafficplanner.cfg`**
```
action_blending: True
blend_start_step: 10000      # 기본 TF로 충분히 수렴 후
blend_anneal_steps: 65000    # 10K→75K step에 걸쳐 점진 증가
blend_target_alpha: 1.0      # 최종 완전 AR
```

**`src/train_trafficplanner.py`**
1. Parser: 4개 blending 파라미터
2. `run_one_epoch()` 내 step 기반 α 계산 (linear ramp)
3. `model.forward(blend_alpha=...)` 전달

**`src/models/trafficplanner_model.py`**
4. `forward()` (line 768): `blend_alpha=0.0` 파라미터
5. `decoder()` (line 1147): `blend_alpha > 0` → `transformer_decoder_training_blended()` 라우팅
6. `_compute_gt_actions()` (line 1838-1886): 연속 state → acc/ddh 역산 → normalize
   - `acc = (speed_next - speed_cur) / dt`, `ddh = (hdot_next - hdot_cur) / dt`
   - Stats: a_stats=(0.409074, 1.045530), ddh_stats=(0.000046, 0.075032), dt=0.5
7. `transformer_decoder_training_blended()` (line 1362-1552): AR loop + blend + bicycle + gradient 유지

---

## Step 기반 Annealing

### 대상: map_attn + blend_alpha (KL은 epoch 기반 유지)

**Map attn annealing** — step 기반 cosine decay:
```python
progress = min(global_step / max(map_attn_anneal_steps, 1), 1.0)
map_attn_w = loss_map_attn * 0.5 * (1 + cos(π * progress))
```
Config: `map_attn_anneal_steps: 62500` (epoch 기반은 fallback으로 유지)

**Blend alpha** — step 기반 linear ramp:
```python
if global_step >= blend_start_step:
    progress = (global_step - blend_start_step) / blend_anneal_steps
    blend_alpha = min(target_alpha, target_alpha * progress)
```

**KL annealing**: epoch 기반 유지 (초기 수 epoch ramp → step 변환 불필요)

**판단 근거**: epoch 기반은 data augmentation/batch_size 변경 시 일정 깨짐. step 기반이 재현성 높음

---

## 학습 코드 수정 내역 (`src/train_trafficplanner.py`)

### Parser 추가 (7개)
```python
--use_adaln True/False              # AdaLN 활성화
--use_a2a_rel_bias True/False       # A2A relative bias 활성화
--loss_z_aux 0.1                    # z_aux loss weight
--action_blending True/False        # action blending 활성화
--blend_start_step 10000            # blending 시작 step
--blend_anneal_steps 65000          # blending annealing 기간
--blend_target_alpha 1.0            # 최종 완전 AR
--map_attn_anneal_steps 62500       # map attn step 기반 annealing (50%)
```

### Model init 수정
```python
model = TrafficPlannerModel(..., use_adaln=cfg.use_adaln, use_a2a_rel_bias=cfg.use_a2a_rel_bias)
```

### Loss weights 추가
```python
loss_weights['z_aux'] = cfg.loss_z_aux  # 0.1
```

### run_one_epoch 수정
- `blend_alpha`, `cfg` 파라미터 추가
- batch loop 내부: step-based blend_alpha, map_attn annealing
- 매 50 step: total + per-module gradient norm logging
- 매 500 step: health metrics logging
- TensorBoard: `anneal/blend_alpha`, `anneal/map_attn_weight` 스칼라

---

## Config 최종 (`configs/train_trafficplanner.cfg`)

```
# 학습 설정
epochs: 300
batch_size: 2

# 데이터
various_500_sample (various_driving_data_20260224에서 500개 랜덤 샘플, seed=42)
→ 425 train scenes, ~833 subseqs, ~416 steps/epoch, ~125K total steps

# V5 Parameters
trans_num_layers: 2          # was 4
trans_ffn_dim: 256           # was 512
use_adaln: True
use_a2a_rel_bias: True
loss_z_aux: 0.1
action_blending: True
blend_start_step: 10000     # step 10K에서 blending 시작
blend_anneal_steps: 65000   # step 10K→75K에서 α: 0→1.0
blend_target_alpha: 1.0     # 최종 완전 AR
map_attn_anneal_steps: 62500 # step 0→62.5K에서 cosine decay → 0 (50%)
```

### 학습 스케줄 (batch_size=2, ~125K total steps 기준)

| 구간 | step | 비율 | Blending α | Map Guidance |
|------|------|------|-----------|-------------|
| 초기 TF 학습 | 0 ~ 10K | 0~8% | 0 (순수 TF) | 100% |
| 전환 구간 | 10K ~ 62.5K | 8~50% | 0 → 0.81 | cosine → 0 |
| AR 증가 | 62.5K ~ 75K | 50~60% | 0.81 → 1.0 | 없음 |
| 완전 AR | 75K ~ 125K | 60~100% | 1.0 | 없음 |

---

## 검증 결과

| 항목 | 결과 |
|------|------|
| Syntax check (py_compile) | ✅ 3 파일 모두 통과 |
| 1 epoch smoke test | ✅ crash 없음, loss 정상 출력 |
| 파라미터 수 | 2,932,174 (V4: 3,846,782 → 23.8% 감소) |
| 학습 속도 | ~7.7 it/s (V4와 유사) |
| Config rollback | ✅ 기본값 = 기존 동작 (모든 V5 feature off) |

---

## Rollback 안전장치

모든 신규 기능의 기본값 = 기존 동작:
```
use_adaln: False          → 기존 LayerNorm
trans_num_layers: 4       → 원래 크기
trans_ffn_dim: 512        → 원래 크기
use_a2a_rel_bias: False   → bias 없음
loss_z_aux: 0.0           → aux decoder loss 없음
action_blending: False    → 순수 TF
```

---

## 다음 단계

1. **테스트 학습**: 500개 샘플 데이터로 300 epoch
2. **TensorBoard 모니터링**: health metrics로 posterior collapse, intent mode collapse, map attn 집중도 확인
3. **blend_alpha 효과 확인**: step 10K 이후 AR loss 변화, step 75K 이후 완전 AR 안정성
4. **결과 확인 후**: 전체 데이터(15,596 scenes)로 본 학습
