# TODO — 260304

## 현재 문제 상황 (260304 분석)

> 300개 데이터 학습 결과: train loss=3.93, val loss=19.72 (5x gap). 순수 과적합.
> 아래는 학습이 안 되는 원인을 추적하면서 발견한 **미해결 이슈**들.

### Issue A: encoder에 global position이 그대로 들어감 — ✅ 확인 & 수정 완료

**근본 원인**: `fit_traffic_model.py`(원본) → `fit_traffic_model_trans.py`(GCN 전환) 시 `transform2frame` 누락.
프로젝트 **초기부터 존재한 버그**로, 이후 `trafficplanner_model.py`(V5)까지 전파됨.

**원본 STRIVE** (`fit_traffic_model.py:471`):
```python
local_past_kin = transform2frame(scene_graph.past[:, -1, :4], scene_graph.past[:, :, :4])
# past도 future도 마지막 past step 기준 local 변환 후 MLP/GRU 입력
```

**GCN 전환 후** (우리 코드):
```python
cur_state = traj_data[:, t, :]  # global 좌표 그대로 MLP에 입력
```

**실측 데이터로 검증한 입력 스케일**:

| 차원 | global (수정 전) | local (수정 후) | 비고 |
|------|----------------|----------------|------|
| x_norm | -16.1 | -0.23 | 70x 감소 |
| y_norm | +27.6 | +0.006 | 4600x 감소 |
| hcos | -0.97 | +1.00 | 비슷 |
| speed | -2.24 | -2.24 | 동일 (scalar) |
| **x/speed 비율** | **7배** | **0.10배** | **균형 회복** |

**수정 내용** (`trafficplanner_model.py` `_run_temporal_encoder()`):
```python
ref_frame = scene_graph.past[:, -1, :4]  # last past step
local_kin = transform2frame(ref_frame, traj_data[:, :, :4])  # (NA, T, 4)
local_traj = torch.cat([local_kin, traj_data[:, :, 4:]], dim=2)  # speed/hdot 유지
# 이후 local_traj를 step_feature_extractor에 입력
# GCN pos는 global 유지 (edge의 transform2frame은 내부 처리)
```

**과적합 메커니즘**: global 절대좌표(x/15=30) → MLP가 "위치 암기" → train OK, val 폭발 (5x gap)
**local 좌표가 과적합에 강한 이유**: 위치 무관한 "행동 패턴"(감속/좌회전 등)만 인코딩 → 일반화 가능

### Issue B: lscale = (0.0, 15.0) — ✅ 검증 완료, 변경 불필요

#### lscale의 역할
- state (x, y, hx, hy, speed, hdot) 중 **x, y 정규화**: `norm_x = (x - 0.0) / 15.0`
- Dataset에서 global 좌표에 바로 적용 → 모델 내부에서 `transform2frame`으로 local 변환
- **mean=0 필수**: NVIDIA 원본이 5회 반복 강조 ("must have mean 0! Make heavy use of this assumption")
  - position은 상대 변위로 사용되므로 global offset을 빼면 안 됨
  - 주행 편향(CARLA future ly mean=-4.3m)은 모델이 학습할 패턴이지 정규화로 제거할 값이 아님

#### CARLA 7798 데이터 실측 (6628 train files, 38326 subsequences)

**transform2frame 후 local 좌표 분포** (past[-1] 기준, 전체 16 step):

| | mean | std | p5 | p95 |
|---|---|---|---|---|
| local_x (종방향) | 0.46m | **14.40m** | -24.7m | 25.8m |
| local_y (횡방향) | -3.04m | **15.67m** | -32.9m | 23.0m |
| **x+y 전체** | **-1.29m** | **15.15m** | — | — |

→ **전체 std = 15.15 ≈ 15.0** — NVIDIA가 nuScenes에서 산출한 값과 거의 동일

#### std=15로 normalized 후 범위

| | past p95 | future dist p95 |
|---|---|---|
| CARLA | 0.80 | 3.01 |
| nuScenes (속도 기반 추정) | ~0.7 | ~3.0 |

→ 두 데이터셋에서 normalized 분포가 유사 → **std=15 유지**

### Issue G: decoder GCN `interaction_gcn`에도 동일한 절대좌표 문제 — ✅ 수정 완료

**위치**: 3곳 모두 수정
- `_run_interaction_gcn_parallel()` (TF 모드)
- blended AR 루프
- AR sampling 루프

**수정 방법**: 인코더와 동일하게 `ref_frame = scene_graph.past[:, -1, :4]` 기준 local 변환
- `scene_graph.x` = local 좌표 기반 node feature (mlp_in 입력)
- `scene_graph.pos` = global 좌표 유지 (edge transform2frame용)
- bicycle model `prev_state` = global 유지 (state propagation + loss 비교)

**ref_frame 고정 이유**:
- 인코더와 동일한 기준점 → MLP 입력 분포 일관성
- past[-1] 고정이면 궤적 형태(직진/커브) 정보가 좌표에 담김
- 매 스텝 기준이면 항상 (~0.2, 0) → 궤적 구분 불가 (speed/hdot과 중복)

**검증 완료**:
- Loss 전부 영향 없음 (pred_future = bicycle model global 출력 → 수정과 무관)
- A2A RelBias: pairwise 차이만 사용 → global/local 동일 (5e-4 오차)
- GCN edge: transform2frame 내부 처리 → global/local 동일 (1e-4 오차)
- Map recrop: prev_state(global) 그대로 사용 → 영향 없음

---

### Transformer 내부 feature 추적 (전수 조사)

#### 절대좌표가 직접 MLP/Transformer feature에 포함되는 곳

| 위치 | 입력 | 절대좌표? | 상태 |
|------|------|----------|------|
| **Encoder** `step_feature_extractor` | state(6d) | ~~예~~ | ✅ 수정 완료 |
| **Decoder** `interaction_gcn.mlp_in` | state(6d)+lw+sem | **예** | 🔴 미수정 |
| **AR loop** `interaction_gcn` (line 1719, 1974) | state(6d) | **예** | 🔴 미수정 (동일 함수) |

#### 상대화되어 안전한 곳

| 위치 | 처리 방식 | 상태 |
|------|----------|------|
| GCN edge (message passing) | `transform2frame(pos_i, pos_j)` 내부 처리 | ✅ 안전 |
| A2A RelBias 8-feature | `pos_j - pos_i` 후 heading 회전 → 전부 상대화 | ✅ 안전 |
| A2S map K/V | CNN feature (좌표 아님) | ✅ 안전 |
| A2Z z K/V | z_global만 사용 | ✅ 안전 |
| Positional Encoding | 시간 인덱스 sin/cos (좌표 무관) | ✅ 안전 |
| Bicycle model | prev_state(global) + (a,ddh) 적분 → global 출력 | ✅ 정상 |
| Recon loss | pred(global) vs GT(global) 비교 | ✅ 정상 |
| Map attn soft label | GT global 독립 계산 (encoder 무관) | ✅ 정상 |

#### A2A RelBias 8개 feature 상세

`_compute_rel_features_static(states)` — 모든 feature가 **상대화됨**:

| # | Feature | 계산 | 절대좌표? |
|---|---------|------|----------|
| 1 | rel_long | (pos_j - pos_i) · heading_i | 상대 |
| 2 | rel_lat | (pos_j - pos_i) × heading_i | 상대 |
| 3 | dist | ‖pos_j - pos_i‖ | 상대 |
| 4 | rel_v_long | (vel_j - vel_i) · heading_i | 상대 |
| 5 | rel_v_lat | (vel_j - vel_i) × heading_i | 상대 |
| 6 | closing | -(dp · dv) / dist | 상대 |
| 7 | ttc | dist / closing | 상대 |
| 8 | rel_heading | atan2(cross, dot) | 상대 |

#### AdaLN 조건

z_global(32dim)을 조건으로 gamma/beta 생성. 좌표 무관.

#### Intent predictor 입력

Layer 0 출력 token(128dim). GCN feature(64, 절대좌표 오염) 포함.
→ Issue G 수정 시 자동 해결.

#### Output head 입력

최종 transformer layer 출력 token(128dim). 동일하게 GCN 오염 포함.
→ Issue G 수정 시 자동 해결.

### Issue C: CARLA_NORM_STATS / BIKE_PARAMS 정확성 — ✅ 검증 완료 (7798 데이터)

#### 검증 과정

1. **xlsx 헤더 ↔ FITDataset 인덱스 매핑**: 28열 전수 검증 ✅
2. **Quaternion 순서**: `Quaternion(ow, ox, oy, oz)` = `(w,x,y,z)` pyquaternion 일치 ✅, norm=1.0 ✅
3. **State normalizer (speed, hdot)**: config 값과 실측 정확히 일치 ✅
4. **BIKE_PARAMS (a_stats, ddh_stats)**: 불일치 발견 → 수정 → **7798 데이터로 최종 검증** ✅

#### a_stats/ddh_stats 문제 (해결됨)

**핵심**: 정규화 통계는 "물리적으로 정확한 가속도 분포"가 아니라,
**모델 `_compute_gt_actions()`가 실제로 계산하는 값의 분포**를 따라야 함.
```python
raw_acc = (speed_seq[:, 1:] - speed_seq[:, :-1]) / 0.5   # dt=0.5 고정, 순수 선형 차분
raw_ddh = (hdot_seq[:, 1:] - hdot_seq[:, :-1]) / 0.5
```

#### 검증 방법

1. `verify_norm_final.py`로 xlsx 직접 읽기 (FITDataset 의존 제거)
2. 300 데이터로 FITDataset 직접 로드 결과와 비교 → **6자리까지 완전 일치** 확인
3. 7798 데이터 (`various_driving_data_20260224`) 전체로 최종 산출

#### 최종값 (7798 데이터, train 85% = 6628 files, 38326 subsequences)

| 항목 | 최종값 |
|------|--------|
| speed | (6.225233, 2.673987) |
| hdot | (0.051012, 0.233765) |
| acc | (0.252031, 1.923014) |
| ddh | (0.003361, 0.371728) |
| l, w | (4.9017, 0.001), (2.1283, 0.001) |
| lscale | (0.0, 15.0) — 변경 불필요 (아래 Issue B 참조) |
| h | (0.0, 1.0) |

수정 파일: `utils.py` (CARLA_BIKE_PARAMS + CARLA_NORM_STATS), `fit_dataset.py` (scenario_path → 7798 데이터)

### Issue D: recon_loss 구조와 pos_weight — ✅ 분석 완료, pos_weight=1로 수정

**분석 결과**:
```
recon_loss = -log_normal(pred, gt, var=1) + recon_pos_weight × pos_mse
           = [4dim 합: 0.5×log(2π) + 0.5×(pred-gt)²] + pos_weight × pos_mse
```

**발견**:
- `-log_normal(var=1)`의 상수항 = `4 × 0.5 × ln(2π)` = **3.676** (학습 불가능한 상수)
- 수렴 시 recon ≈ 3.83 → 학습 가능한 부분은 0.15 (상수 대비 4%)
- 정규화 스케일: position은 `/15`로 MSE ~`1/225`, heading은 `/1`로 MSE ~`1`
- pos_weight=100일 때: position gradient가 heading 대비 **155배** → heading 학습 불능
- pos_weight=0일 때: recon gradient가 너무 작아 auxiliary loss(intent_ce 등)가 지배

**수정**: `recon_pos_weight: 100 → 1`
- pos_weight=1: position/heading gradient 비율 ≈ **1.3:1** (근사 균형)
- 순수 `var=1 log_normal`만으로도 4dim(x,y,hcos,hsin) 전부 학습 가능

### Issue E: z_aux_loss — ✅ 분석 완료, weight=0으로 비활성화

**분석 결과**:
- train z_aux=0.092, val z_aux=0.919 (**10.1x gap**) → z_aux MLP가 학습 데이터 암기
- NVIDIA 원본 STRIVE: z에 대한 auxiliary loss **없음** (KL + reconstruction만으로 충분)
- z_aux MLP가 독립적으로 과적합 → encoder에 잘못된 gradient 전파
- z_global의 정보 보유 여부는 KL divergence로 충분히 모니터링 가능

**수정**: `loss_z_aux: 0.5 → 0.0` (코드 유지, weight만 0)
- z_aux MLP 구조는 코드에 남겨둠 (향후 모니터링용으로 detach 후 활용 가능)
- z_global 학습은 별도 정규화 전략(아래 섹션 8~9)으로 대체

### Issue F: 300 데이터 과적합 (확인됨)

- 300 시나리오 (~736 샘플) vs 3.5M params → 극심한 과적합
- train: pos_err ~0.67m, val: pos_err ~5.9m
- 10K 데이터 전환이 근본 해결이지만, 위 이슈들이 해결 안 되면 10K에서도 문제 발생 가능

---

## 완료 (260301에서 이어짐)

### 1. CARLA 정규화 적용
- [x] NUSC_NORM_STATS → CARLA_NORM_STATS 전환 (7개 파일)
- [x] CARLA_BIKE_PARAMS 적용 (bicycle model)
- [x] 차량 치수 Lincoln MKZ 2017 반영

### 2. Intent 격자 범위 확장
- [x] prototype [-1, 0, 1] → [-2, 0, 2]
- [x] `intent_range: 2.0` config 파라미터화
- [x] loss.py, viz_gt_labels.py, train.py 반영

### 3. Intent L/R 라벨 수정
- [x] viz_gt_labels.py xticklabels ['L','0','R'] → ['R','0','L']
- [x] hdot>0 = 좌회전 = 양수 = L (CARLA 좌표계)

---

## 260304 수정 완료

### 6. Intent Codebook inference soft 통일
- [x] inference 시 `one_hot(argmax)` → `softmax(logits / temperature)` 변경
- [x] train/inference gap 해소 (둘 다 soft weights → codebook 가중합)
- 파일: `trafficplanner_model.py` IntentCodebook.forward()

### 7. Action Blending 개선
- [x] blend_alpha_floor 도입: step 0부터 α=0.05로 시작
- [x] 완전 선형 ramp: `α = floor + (target - floor) * min(step / anneal_steps, 1.0)`
- [x] 순수 TF 구간 제거 → TF→AR 전환 충격 없음
- [x] blend_start_step 제거 (불필요)
- 파일: `train_trafficplanner.py`, `train_trafficplanner.cfg`

### 8. z_global 정규화 전략 (Issue D, E 해결 포함)

**배경**: 이전 학습(300 epoch)에서 z_global KL = 9.8→130→2.3 (posterior collapse 패턴)

- [x] z_aux weight: 0.5 → **0.0** (Issue E: 10.1x 과적합, NVIDIA 원본에도 없음)
- [x] enc_dropout: 0.1 → **0.0** (인코더 PE dropout off — local 좌표 전환으로 과적합 위험 감소)
- [x] dec_past_dropout: 0.0 → **0.5** (디코더 past context dropout — z_global 의존도 강제)
- [x] kl_free_bits: **0.05** 도입 (per-dim free nats — posterior collapse 방지)
  - `kl_per_dim = max(kl_per_dim - 0.05, 0)` → 각 dim이 0.05 nats까지 penalty-free
- [x] kl_start_epoch: 50 → **20** (KL annealing 시작 앞당김)
- [x] kl_anneal_end: 65 → **40** (KL annealing 종료 앞당김)
- 파일: `common.py`, `trafficplanner_loss.py`, `trafficplanner_model.py`, `train_trafficplanner.py`, `.cfg`

### 9. Loss weight 재설계

**배경**: recon(3.83) 내 상수항 3.676이 96% 차지 → gradient 비율 왜곡

- [x] recon_pos_weight: 100 → **1** (Issue D: pos/head gradient 1.3:1 균형)
- [x] loss_intent_ce: 0.05 → **0.005** (수렴 시 65.9% gradient 지배 → 적정 비율로)
- [x] loss_map_attn: 0.05 → **0.005** (intent_ce와 동일 비율 유지)
- [x] loss_sur_pred / loss_ego_pred: **0.1** 유지 (적정)

**최종 config 요약**:
```
loss_kl: 0.004, kl_start_epoch: 20, kl_anneal_end: 40, kl_free_bits: 0.05
enc_dropout: 0.0, dec_past_dropout: 0.5
recon_pos_weight: 1, loss_z_aux: 0.0
loss_intent_ce: 0.005, loss_map_attn: 0.005
loss_sur_pred: 0.1, loss_ego_pred: 0.1
```

---

## 학습 준비 (다음)

### 8. 데이터 1만개로 전환
- [ ] `various_300_sample` → 전체 데이터셋으로 config 변경
- [ ] batch_size, num_workers 조정 검토
- [ ] blend_anneal_steps, lr_total_steps 등 데이터 크기에 맞게 재계산

---

## 학습 후 / 다음 단계

### 4. Debug Distribution 시각화
- [ ] 학습 중 intent/map_attn/z_kl 분포 시각적 디버깅 시스템
- `debug_distribution: True` 설정 시 주기적으로 이미지 저장
- 상세 설계는 아래 참조

### 5. Intent 연속화 (적대적 최적화용) — 보류
- [ ] 현재: 이산 9-class codebook + Gumbel-Softmax → inference 시 softmax (수정 완료)
- [ ] 당초 목표: codebook 제거, z_local을 직접 MLP로 연속 2D (acc, yaw) 생성
- **문제점**: 연속 z_local → Linear → (acc, yaw)가 output head와 역할 중복
  - output head: token(128) → Linear → (acc, yaw)
  - z_local MLP: token(128) → MLP → z_local(32) → Linear → (acc, yaw)
  - "의도 표현" vs "action 직접 예측" 구분이 사라짐
  - codebook의 역할 분리 (what vs how much)가 소실됨
- **현재 결론**: codebook 유지, 적대적 최적화는 δ_logits 방식으로 대체 (아래 참조)
- [ ] 시점: 적대적 최적화 결과가 불충분할 경우 재검토

---

## 적대적 시나리오 최적화 계획

### 개요
- 목적: 학습된 모델(weight 고정)에서 z_global + z_local을 탐색하여 위험 시나리오 생성
- 방식: gradient-based search (학습이 아닌 검색)

### 최적화 대상

#### z_global (32dim, continuous)
- CVAE posterior에서 샘플링된 latent vector
- **전체 trajectory 방향** 결정 (직진, 좌/우회전 등)
- continuous → gradient 최적화 자유로움
- 메인 최적화 대상

#### z_local (δ_logits 방식, 9dim × FT steps)
- intent_predictor가 출력한 logits에 **δ (perturbation)** 를 가산
- `logits_perturbed = logits + δ` → softmax → codebook 가중합 → z_local
- **step별 행동 반응** 조작 (감속/가속/조향 비율 변경)
- δ는 continuous → gradient 최적화 가능
- codebook 9개 벡터의 가중합 범위 안에서 탐색
  - 3(acc) × 3(yaw) 격자의 convex combination = 격자 내부 전체 커버
  - 가속도 [-2, +2], yaw [-2, +2] 범위에서 연속적 제어 가능

### 최적화 흐름

```
입력: 학습된 모델 M (frozen), 시나리오 scene_graph

최적화 변수:
  z_global: (NA, 32)         ← learnable
  δ_logits: (FT, N_ego, 9)   ← learnable

Forward:
  1. z_global → decoder tokens
  2. Layer 0 → intent_predictor → logits
  3. logits + δ_logits → softmax → codebook weights → z_local
  4. z_local concat → Layer 1 → output head → bicycle model → trajectory

Loss (예시):
  L_collision: ego-sur 충돌 근접도 (minimize distance)
  L_plausibility: z_global의 prior likelihood (너무 비현실적 방지)
  L_smooth: δ_logits의 시간축 smoothness (급격한 intent 전환 방지)

  total = L_collision + λ₁ * L_plausibility + λ₂ * L_smooth

Gradient:
  ∂L/∂z_global, ∂L/∂δ_logits → Adam 등으로 업데이트
  (softmax, codebook matmul 모두 미분 가능 → gradient chain 정상)
```

### 주의사항
- δ_logits가 극단적으로 커지면 one-hot에 가까워짐 → L2 regularization 고려
- z_global이 prior 분포에서 너무 벗어나면 비현실적 시나리오 → KL penalty
- codebook 벡터가 충분히 다양하지 않으면 탐색 범위 제한 → 학습 후 codebook 분석 필요

---

## Debug Distribution 시각화 상세

### 시각화 대상

#### 1. Intent (9-way soft label)
- Model predicted: 3×3 heatmap (softmax weights)
- GT soft label: 3×3 heatmap (acc/yaw Gaussian softmax)
- 배경: 맵 + GT 경로 + pred 경로, 현재 step 강조

#### 2. Map Attention (57×57 token grid)
- Model attn: heatmap (attention weights → grid reshape)
- GT soft label: heatmap (GT future position 기반)
- 배경: 맵 overlay + 경로

#### 3. z_global KL (prior vs posterior)
- Per-dim KL: 32-dim bar chart
- Prior/Posterior mean ± std 비교
- active_dims, total KL 수치

### 저장 주기

| 모드 | 주기 | 시나리오 선택 |
|------|------|-------------|
| train | 200 step마다 | 배치 내 랜덤 1개 |
| val | val 할 때마다 | 배치 내 랜덤 1개 |
| test | 매 배치 | 매번 |

### 구현 순서
1. `src/utils/debug_viz_distribution.py` 생성
2. Config parser에 `debug_distribution: True/False` 추가
3. train.py / test.py 연동

### 데이터 접근 경로

| 필요 데이터 | 접근 방법 |
|------------|----------|
| Intent weights (model) | `model.get_intent_weights()` |
| Intent GT soft label | loss.py 로직 재활용 |
| Map attn weights | `model.get_ego_map_attn_weights()` → reshape |
| Map attn GT soft label | `loss_fn._make_soft_label()` |
| z_global prior/posterior | `model._last_prior_out`, `model._last_posterior_out` |
| GT trajectory | `scene_graph.future` (denormalize) |
| Pred trajectory | `pred['future_pred']` (denormalize) |
| Map image | `map_env` 객체에서 crop |

### 주의사항
- TF mode에서 intent/attn은 shifted: `[PT-1:-1]` 슬라이싱
- 좌표계 일치: map_recrop=True면 각 step 기준 crop
- matplotlib Agg backend 사용
