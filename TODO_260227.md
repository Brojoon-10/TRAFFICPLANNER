# TODO — V5 모델 리디자인 (260227)

## 구현 순서

모든 변경을 동시 구현. Action blending은 alpha=0(순수 TF)에서 시작하므로 단계 분리 불필요.

---

### 1. Health Monitoring (Change 5) — 진단 기반 먼저 구축
- [ ] `forward()`에 z_samp, prior/posterior mu/var detach 저장 (`trafficplanner_model.py:603`)
- [ ] `compute_health_metrics()` 메서드 구현 (z_global, intent, map_attn 지표)
- [ ] `run_one_epoch()`에 gradient norm 로깅 추가 (매 50 step, `train_trafficplanner.py`)
- [ ] epoch loop에 health metrics 로깅 추가 (`train_trafficplanner.py`)
- [ ] 검증: TensorBoard에 `health/*`, `grad_norm/*` 스칼라 표시 확인

### 2. Decoder 축소 (Change 2)
- [ ] `train_trafficplanner.cfg:142` → `trans_num_layers: 2`
- [ ] `train_trafficplanner.cfg:145` → `trans_ffn_dim: 256`
- [ ] 검증: 모델 로드 및 실행, 파라미터 수 감소 확인

### 3. AdaLN (Change 1)
- [ ] `AdaLN` 클래스 구현 (`trafficplanner_model.py`, TransDecoderLayer 앞)
  - `__init__(d_model, z_size)`: LN + MLP(z→gamma,beta), identity init
  - `forward(x, z_cond)`: gamma * LN(x) + beta
- [ ] `TransDecoderLayer.__init__`에 `use_adaln, z_size` 파라미터 추가
- [ ] 5개 LayerNorm → 조건부 AdaLN 교체:
  - [ ] `a2t_norm` (line 158)
  - [ ] `a2a_norm` (line 162)
  - [ ] `a2s_norm` (line 166)
  - [ ] `ego_ffn_norm` (line 179)
  - [ ] `sur_ffn_norm` (line 187)
- [ ] `TransDecoderLayer.forward`에 `z_global=None` 파라미터 추가 (line 257)
  - [ ] A2T: z_global unsqueeze(1) → T broadcast
  - [ ] A2A: z_global unsqueeze(0) → BT broadcast
  - [ ] A2S: A2A와 동일
  - [ ] FFN ego: z_global[ego_idx] unsqueeze(0).unsqueeze(0)
  - [ ] FFN sur: z_global[sur_idx] unsqueeze(0).unsqueeze(0)
- [ ] `TrafficPlannerModel.__init__`에 `use_adaln` 추가 (line 368)
- [ ] TransDecoderLayer 생성 시 `use_adaln, z_size` 전달 (line 544-548)
- [ ] `transformer_decoder_training` (line 1017): `z_global=z` 전달
- [ ] `transformer_decoder_inference`: `z_global=z_flat` 전달
- [ ] config parser + cfg에 `use_adaln: True` 추가
- [ ] 검증: `use_adaln=False` → 기존과 동일 출력

### 4. A2A Relative Bias (Change 3)
- [ ] `A2ARelativeBias` 클래스 구현: MLP 8→32→nhead, zero-init 출력
- [ ] `_compute_a2a_rel_features()` 메서드 구현
  - [ ] rel_long, rel_lat (heading unit vector로 회전)
  - [ ] dist (유클리드, eps 포함)
  - [ ] rel_v_long, rel_v_lat (속도 벡터 회전)
  - [ ] closing_speed = -(Δpos·Δvel)/dist
  - [ ] ttc = dist / max(closing, 0.1), clamp(0,10)
  - [ ] rel_heading = atan2(cross, dot)
- [ ] **단위 테스트** (필수 — TTC edge case)
  - [ ] Agent(0,0) heading(1,0) speed=10 vs Agent(5,0) heading(0,1) speed=5
  - [ ] 기대값 확인: rel_long=5, rel_lat=0, dist=5, closing=10, ttc=0.5, rel_heading=π/2
  - [ ] Self-pair 테스트: 모든 feature ≈ 0
  - [ ] 멀어지는 agent 테스트: closing<0, ttc=10(clamp)
- [ ] `SharedKVAttention._attention` (line 81): `rel_bias=None` 추가, softmax 전 더하기
- [ ] `SharedKVAttention.forward` (line 102): `rel_bias=None`, ego/sur 행 슬라이싱
- [ ] `TransDecoderLayer.__init__`: `use_a2a_rel_bias` → bias 모듈 생성
- [ ] `TransDecoderLayer.forward`: `agent_states=None` → rel_features 계산 → bias → a2a_attn 전달
- [ ] `transformer_decoder_training`: all_states를 layer에 전달
- [ ] `transformer_decoder_inference`: 매 step 현재 states로 계산
- [ ] config parser + cfg에 `use_a2a_rel_bias: True` 추가
- [ ] 검증: `use_a2a_rel_bias=False` → 기존과 동일 출력

### 5. z_global Aux Decoder (Change 4)
- [ ] `__init__`에 `z_aux_step_embed = Embedding(12, 32)` 추가
- [ ] `__init__`에 `z_aux_decoder = MLP(64→128→128→2)` 추가
- [ ] 학습 경로에서 z_aux_traj 계산 (ego z + step_embed → MLP)
  - [ ] `self._z_aux_traj` 저장
- [ ] `trafficplanner_loss.py`에 z_aux_loss 추가
  - [ ] `F.mse_loss(z_aux_traj, gt_actions_ego)` (action space MSE)
  - [ ] GT action은 `_compute_gt_actions()`로 계산 (Change 6과 공유)
- [ ] cfg에 `loss_z_aux: 0.1` 추가 + parser
- [ ] 검증: z_aux_loss가 학습 중 감소하는지 확인

### 6. Action Blending (Change 6)
- [ ] `_compute_gt_actions()` 헬퍼 구현
  - [ ] 연속 state unnormalize → acc = (speed_next - speed_cur) / dt
  - [ ] ddh = (hdot_next - hdot_cur) / dt (heading rate의 **변화량**)
  - [ ] action space normalize: (acc - a_mean) / a_std, (ddh - ddh_mean) / ddh_std
  - [ ] `_apply_dynamics()` (line 1395-1413)의 정확한 역연산인지 교차검증
- [ ] `forward()` 시그니처에 `blend_alpha=0.0` 추가
- [ ] decoder 분기: `blend_alpha > 0` → `transformer_decoder_training_blended()`
- [ ] `transformer_decoder_training_blended()` 구현
  - [ ] AR loop (템플릿: `transformer_decoder_inference`, line 1125-1346)
  - [ ] 매 step: decoder 출력 + GT action blend → bicycle model → 다음 state
  - [ ] gradient 유지 (detach 안 함!)
  - [ ] aux loss 수집 (sur_pred, ego_pred, intent, map_attn) 동일 방식
- [ ] cfg에 blending 4개 파라미터 추가
- [ ] `run_one_epoch()`에 step 기반 alpha 계산
- [ ] `model.forward()`에 `blend_alpha` 전달
- [ ] 검증: `blend_alpha=0` → 순수 TF와 동일 출력
- [ ] 검증: `blend_alpha=0.001` → loss 거의 동일

### 7. Step 기반 Annealing
- [ ] Map attn annealing: epoch loop (line 750-757) → `run_one_epoch()` 이동, global_step 사용
  - [ ] `progress = global_step / map_attn_anneal_steps`
  - [ ] Cosine decay: `weight = base * 0.5 * (1 + cos(π * progress))`
- [ ] cfg 변경: `map_attn_anneal_epochs: 300` → `map_attn_anneal_steps: 150000`
- [ ] KL annealing: epoch 기반 유지 (변경 없음)
- [ ] 검증: map_attn weight가 step에 따라 정상 감소

### 8. Config & Parser 정리
- [ ] `train_trafficplanner.cfg`에 모든 새 파라미터 추가:
  ```
  use_adaln: True
  use_a2a_rel_bias: True
  loss_z_aux: 0.1
  action_blending: True
  blend_start_step: 50000
  blend_anneal_steps: 100000
  blend_target_alpha: 0.5
  map_attn_anneal_steps: 150000
  ```
- [ ] `train_trafficplanner.py` parser에 모든 파라미터 추가
- [ ] 기존 `map_attn_anneal_epochs` 제거

---

## 최종 검증

- [ ] Smoke test: 1 epoch 학습 crash 없음
- [ ] 파라미터 수 확인 (~2.5M 목표)
- [ ] TensorBoard: `health/*`, `grad_norm/*` 스칼라 표시
- [ ] Action blending alpha: 0에서 시작, step에 따라 증가
- [ ] Map attn weight: 0.1에서 시작, step에 따라 cosine 감소
- [ ] KL weight: 기존 epoch 기반 유지

---

## 핵심 참조 코드

| 항목 | 위치 |
|------|------|
| TransDecoderLayer | `src/models/trafficplanner_model.py:148-317` |
| SharedKVAttention | `src/models/trafficplanner_model.py:54-142` |
| TF decoder (병렬) | `src/models/trafficplanner_model.py:949-1123` |
| AR decoder (추론) | `src/models/trafficplanner_model.py:1125-1346` |
| _apply_dynamics | `src/models/trafficplanner_model.py:1395-1413` |
| car_dynamics | `src/models/common.py:47-68` |
| kl_normal | `src/losses/common.py:8-24` |
| NUSC_BIKE_PARAMS | `src/datasets/utils.py:121` |
| kinematics2angle/vec | `src/utils/transforms.py` |
| Training loop | `src/train_trafficplanner.py` |
| Config | `configs/train_trafficplanner.cfg` |

## 핵심 주의사항

- **코드 주석은 반드시 영어로**
- **ddh = heading rate(hdot)의 변화량** — heading 자체가 아님
- **TF-AR 일관성**: PE N 출력 → future step (N-PT+1) 예측
- **Loss shifted indexing**: `x[:, PT-1:-1]` (not `x[:, PT:]`)
- **A2A rel features 반드시 단위 테스트** — TTC edge case 실수 주의
- **Action blending에서 gradient 유지** — detach 절대 안 됨
- 모든 기본값 = 기존 동작 (안전한 rollback)
