# TODO 2026-02-19: z_local 구조 개선 + GPU 최적화 + 인코더 강화

## 목표

- z_local의 interaction 모델링 능력 강화
- CPU-GPU 전환 병목 제거로 학습 속도 향상
- 학습 코드/loss 구조는 변경하지 않음 (모델 코드만 수정)
- autoregressive 구조 유지
- Prior/Posterior 인코더 표현력 강화 (pretrain 품질 향상)
- z_local 윈도우 확대로 더 풍부한 temporal context 제공

## 수정 대상 파일

- `src/models/trafficplanner_model.py` (모델 코드만)
- `src/test_finetune_trafficplanner.py` (z_local_var 참조 호환성만)

## 백업

- [X] `trafficplanner_model.py.backup_20260219` 생성 완료

---

## 변경 1: Multi-layer Cross-Attention (1층 → 2층)

### 근거

- **UniAD MotionFormer** (CVPR 2023 Best Paper): 3-layer transformer decoder, 8 heads, dim=256
  - https://github.com/OpenDriveLab/UniAD
- **GameFormer** (ICCV 2023 Oral): 4-level hierarchical decoder, 8 heads, dim=256
  - https://github.com/MCZhi/GameFormer
- 우리 모델: 1-layer, 4 heads, dim=64 → 최소 기준에도 못 미침
- dim이 64로 작으니 2층이 적절 (3층은 overfitting 위험)
- residual connection + LayerNorm은 모든 검증된 모델의 기본 구성

### 구현

- [X] `self.cross_attn` → `self.cross_attn_1` + `self.cross_attn_2` 분리 (모듈 정의부)
- [X] `self.cross_attn_norm_1`, `self.cross_attn_norm_2` (LayerNorm) 추가
- [X] `_compute_z_local()`에서 2-layer 적용 (함수명도 변경 완료):
  ```
  attn1_out = cross_attn_1(Q, KV, KV) + Q   # residual
  attn1_out = cross_attn_norm_1(attn1_out)   # LayerNorm
  attn2_out = cross_attn_2(attn1_out, KV, KV) + attn1_out  # residual
  attn2_out = cross_attn_norm_2(attn2_out)   # LayerNorm
  ```
- [X] non-mult_samp / mult_samp 두 경로 모두 수정

### 학습/loss 영향: 없음

- cross-attention 출력 shape 동일 (64차원)
- 하위 흐름 변경 없음

---

## 변경 2: VAE 샘플링 제거 → Deterministic z_local

### 근거

- **UniAD**: interaction cross-attention 출력을 직접 사용 (VAE 없음)
- **GameFormer**: cross-attention → MLP → trajectory (VAE 없음)
- **VADv2**: probabilistic이지만 trajectory vocabulary 기반, latent VAE가 아님
- interaction 모듈에서 VAE 샘플링하는 검증된 모델이 없음
- VAE 병목 문제: 64차원 → 32차원 mean+logvar 압축 → 샘플링 노이즈 → 정보 손실
- z_local에 KL loss가 없어서 VAE 구조의 이점(regularization)도 활용 안 되고 있음

### 구현

- [X] `self.z_local_mlp` (64→64→32×2) 삭제 → `self.z_local_proj` (nn.Linear, 64→32) 교체
- [X] `_compute_z_local_dist()` 반환값 변경:
  - 기존: `(z_local_mean, z_local_var)` + 외부에서 `rsample(mean, var)`
  - 변경: `z_local` 직접 반환 (32차원, deterministic)
  - 함수명도 `_compute_z_local()`로 변경 (dist 아님)
- [X] `autoregressive_decoder()`에서 `rsample` 호출 제거
- [X] `teacher_forcing_decoder()`에서 `rsample` 호출 제거

### 학습/loss 영향: 없음

- z_local에 KL loss 없음 (확인 완료)
- sparsity loss는 default=False로 비활성화 상태
- `z_combine_mlp` 입력 shape 동일 (z_global 32 + z_local 32 = 64)
- decoder 이하 흐름 완전 동일

---

## 변경 3: GPU 최적화 (_compute_z_local_dist 내)

### 근거

- `.item()` 호출이 GPU→CPU 동기화를 강제 (배치당 8~16회)
- Python for-loop가 GPU 배치 병렬화를 방해
- list append → torch.stack이 불필요한 메모리 재할당 유발

### 구현

- [X] `scene_graph.ptr`를 루프 전 한번에 CPU로: `ptr_cpu = scene_graph.ptr.cpu().tolist()`
- [X] `.item()` 호출 전부 제거 → `ptr_cpu[b]`, `ptr_cpu[b+1]` 사용
- [ ] sliding window: `list.pop(0)` → `collections.deque(maxlen=W)` 사용 (미구현 — autoregressive/TF decoder 내부)
  - `deque`는 양쪽 끝 O(1), list.pop(0)은 O(n)

### 학습/loss 영향: 없음 (순수 속도 최적화)

---

## 변경 4: z_local=0 Ablation 플래그

### 근거

- z_local이 실제로 기여하는지 검증하는 가장 확실한 방법
- 정상 모델 vs z_local=0 모델 성능 비교 → z_local 기여도 정량 측정

### 구현

- [X] `self.use_z_local = True` 플래그 추가 (모듈 정의부)
- [X] `autoregressive_decoder()`에서: `use_z_local=False`면 z_local=0 텐서 사용
- [X] `teacher_forcing_decoder()`에서: 동일

### 학습/loss 영향: 없음 (ablation 시에만 활성화)

---

## 변경 5: Getter 메서드 및 호환성

### 구현

- [X] `get_z_local_mean()` → z_local 값 직접 반환 (deterministic이므로 mean=actual)
- [X] `get_z_local_var()` → None 반환 (var 없음)
- [X] `_z_local_mean_outputs` / `_z_local_var_outputs` → `_z_local_outputs`로 통합
- [X] `test_finetune_trafficplanner.py`에서 z_local_var 참조하는 곳 수정
  - z_local_var_scene 관련 CSV/PNG: var=None이면 skip

---

---

## 변경 6: z_local 압축 제거 — 64차원 직접 전달 (방법 2 완전 적용)

### 근거

- 현재: cross-attention 출력 64dim → `z_local_proj` (Linear 64→32) → z_local 32dim
- 32차원으로 압축하면 cross-attention이 추출한 interaction 정보가 절반 손실됨
- UniAD/GameFormer: cross-attention 출력(256dim)을 그대로 decoder에 전달, 중간 압축 없음
- 방법 2 원래 제안: 64차원을 그대로 z_local로 사용

### 수정 범위 (모델 코드만)

- `self.z_local_proj` 삭제
- `self.z_local_out_size = 64` 추가 (기존 `z_local_size=32`와 구분)
- `z_combine_mlp` 입력: 32+32=64 → 32+64=**96**
- `_compute_z_local()` 내 zero 텐서 크기 6곳: `z_local_size` → `z_local_out_size`
- `_compute_z_local()` 내 `z_local_proj()` 호출 2곳 제거
- getter 메서드 반환 shape: 32 → 64
- `test_finetune_trafficplanner.py`: z_local CSV/PNG 32차원 → 64차원 (자동 대응 가능하면 수정 불필요)

### 학습/loss 영향: 없음

- loss 코드는 z_local 차원을 직접 참조하지 않음
- `model.get_z_local()` 반환 tensor의 shape만 바뀜 (sparsity loss에서 사용 시 자동 대응)
- `z_combine_mlp`은 모델 내부이므로 모델 코드만 수정

---

## 변경 7: Self-attention 기반 상호작용 추가 (방법 3)

### 근거

- **UniAD MotionFormer**: agent간 self-attention으로 상호 참조 (ego↔sur, sur↔sur)
- **GameFormer**: interaction module에서 모든 agent pair 간 attention 수행
- 현재: ego→sur **일방향** cross-attention만 있음. sur는 ego를 안 보고, sur끼리도 안 봄
- self-attention 추가 시: ego와 sur가 서로를 참조하여 더 풍부한 interaction 정보 추출

### 구현 방식

- cross-attention (변경 1) 이후에 self-attention 레이어 1층 추가
- ego query 결과 + sur의 temporal feature를 합쳐서 self-attention 수행
- self-attention 후 ego 토큰만 추출하여 z_local로 사용
- 새 모듈: `self.interaction_self_attn` (nn.MultiheadAttention) + `self.interaction_self_attn_norm` (LayerNorm)

### 수정 범위 (모델 코드만)

- 모듈 정의부: `self.interaction_self_attn`, `self.interaction_self_attn_norm` 추가
- `_compute_z_local()` 내 non-mult_samp / mult_samp 두 경로:
  - cross-attn 출력 후 ego_token(1) + sur_tokens(num_other) 합침
  - self-attention 수행
  - ego 토큰 (index 0) 추출
- scene별 for-loop 내에서 처리 (현재 구조와 동일)

### 학습/loss 영향: 없음

- z_local 출력 shape 변경 없음 (self-attn은 dim 유지)
- self-attention은 cross-attention 뒤에 추가되는 것이므로 기존 흐름 그대로

### 주의사항

- ego 토큰이 index 0임을 보장해야 함 (concat 순서 고정)
- sur agent 수가 0인 경우 self-attention skip (기존 zero 반환 로직 유지)

---

## 수정 순서

### 이미 완료 (변경 1~5)
1. ~~`_compute_z_local_dist()` → `_compute_z_local()` 전면 재작성 (변경 1+2+3 동시 적용)~~
2. ~~`autoregressive_decoder()` 호출부 수정 (변경 2+4)~~
3. ~~`teacher_forcing_decoder()` 호출부 수정 (변경 2+4)~~
4. ~~getter 메서드 수정 (변경 5)~~
5. ~~`test_finetune_trafficplanner.py` 호환성 수정~~

### 추가 작업 (변경 6+7) — 완료
6. ~~`z_local_proj` 제거 + `z_local_out_size=64` + `z_combine_mlp` 입력 96 (변경 6)~~
7. ~~`interaction_self_attn` 모듈 추가 + `_compute_z_local()`에 self-attn 단계 추가 (변경 7)~~
8. ~~zero 텐서 크기 및 getter 메서드 업데이트~~
9. ~~`test_finetune_trafficplanner.py` 64차원 호환성 확인 (`z_dim` → `z_global_dim`/`z_local_dim` 분리)~~

### 추가 작업 (변경 8~10) — 인코더 강화 + 윈도우 확대 — 완료
10. ~~Prior GRU 레이어 증가 1→2층 (변경 8)~~
11. ~~Posterior Transformer: 이미 nhead=8, nlayer=3 (UniAD 동일) — 수정 불필요 (변경 9)~~
12. ~~z_local 윈도우 확대: pop(0) 제거 → 전체 이력 누적 (변경 10)~~
13. ~~호환성 확인 — shape 변경 없음, 테스트 코드 수정 불필요~~

---

## 변경 8: Prior GRU 레이어 증가 (1층 → 2층)

### 근거

- 현재: `prior_temporal_gru`가 1-layer GRU → past 4스텝의 temporal 패턴 추출 능력 제한
- 2-layer GRU: 하위층이 low-level temporal feature, 상위층이 high-level context 추출
- GameFormer/UniAD: temporal encoding에 multi-layer 사용
- **pretrain 시 학습**, fine-tuning 시 frozen — pretrain 품질 향상이 fine-tuning 출발점을 높임

### 구현

- [X] `self.prior_temporal_gru = nn.GRU(..., 1, ...)` → `nn.GRU(..., 2, ...)`
- [X] 입출력 shape 동일 (GRU num_layers는 hidden state 크기에만 영향)
- [X] `_run_temporal_encoder()`는 수정 불필요 (GRU 출력 shape 동일)

### 학습/loss 영향: 없음

- GRU 출력 shape (NA, PT, D) 동일
- context_vector shape (NA, D) 동일
- prior_in → latent_prior_net 흐름 변경 없음

---

## 변경 9: Posterior Transformer — 수정 불필요

### 확인 결과

- 모델 기본값: `transformer_nhead=8`, `transformer_nlayer=3`
- UniAD MotionFormer와 동일한 설정 (3-layer, 8-head)
- `d_model=64`에 8-head → head당 8dim, 적절한 비율
- **이미 충분한 capacity이므로 수정 불필요**

---

## 변경 10: z_local 윈도우 크기 확대 (고정 4 → 동적 확장)

### 근거

- 현재: `z_local_window=4` 고정 → 항상 최근 4스텝만 사용
- `list.pop(0)`으로 가장 오래된 것 제거 → sliding window
- 문제: autoregressive 후반 스텝에서 이전 정보가 완전히 유실됨
  - t=11에서는 t=0~7의 정보를 전혀 볼 수 없음
- 제안: pop(0) 제거 → window를 계속 누적하여 전체 past + 예측 이력 활용
  - t=0: 4스텝 (past 4)
  - t=5: 9스텝 (past 4 + pred 5)
  - t=11: 15스텝 (past 4 + pred 11)
- z_local_temporal_gru가 가변 길이 시퀀스를 자연스럽게 처리 (GRU는 길이 무관)
- **fine-tuning 시에도 trainable** — z_local 관련 모듈이므로 직접 효과 있음

### 구현

- [X] `autoregressive_decoder()` 내 ego_feat_window/other_feat_window의 `pop(0)` 제거
- [X] `teacher_forcing_decoder()` 내 동일 수정
- [X] `z_local_window` 파라미터는 초기 warmup 크기로만 사용 (더 이상 max 크기 제한 아님)
- [X] `_compute_z_local()` 내 GRU temporal encoding은 가변 길이 자동 대응 (수정 불필요)

### 학습/loss 영향: 없음

- z_local 출력 shape 동일 (64dim)
- GRU는 시퀀스 길이에 무관하게 동일 차원 출력
- temporal GRU의 last hidden state만 사용하므로 shape 변경 없음

### 주의사항

- 윈도우가 커지면 GRU 연산량 증가 (최대 15스텝 vs 기존 4스텝)
- 하지만 per-scene loop 내부이므로 전체 학습 속도에 미치는 영향은 미미
- cross-attention의 KV 길이가 늘어남 → attention이 더 다양한 temporal context 참조

---

## 주의사항

- 모델 코드(trafficplanner_model.py) 내에서만 구조 수정
- 학습 코드 / loss 코드 변경 없음
- autoregressive 구조 유지
- 기존 checkpoint는 호환 안 됨 — 재학습 필요
- 변경 8, 9는 pretrain에만 영향 (fine-tuning 시 frozen)
- 변경 10은 fine-tuning에서도 직접 효과 (z_local은 trainable)
