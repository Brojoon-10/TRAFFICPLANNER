# TODO — Debug Distribution 시각화 시스템 (260301)

## 목표

학습/검증/테스트 중 **분포 학습 상태**를 시각적으로 디버깅.
`debug_distribution: True` 설정 시 `{out}/debug_distribution/{train,val,test}/` 에 이미지 저장.

---

## 시각화 대상 (분포를 학습하는 3가지)

### 1. Intent (9-way soft label)
- **Model predicted**: 3×3 heatmap (Gumbel-Softmax output weights)
- **GT soft label**: 3×3 heatmap (acc/yaw 기반 Gaussian softmax)
- **배경**: 맵 + GT 경로 (파란선) + pred 경로 (빨간선)
- **현재 step 강조**: 해당 step의 GT 위치 ★, pred 위치 ●
- 축 레이블: acc (-1~1), yaw_rate (-1~1)

### 2. Map Attention (29×29 token grid)
- **Model attn**: 29×29 heatmap (attention weights를 grid로 reshape)
- **GT soft label**: 29×29 heatmap (GT future position 기반)
- **배경**: 맵 위에 overlay
- **GT/pred 경로 + 현재 step 강조**

### 3. z_global KL (prior vs posterior)
- **Per-dim KL**: 32-dim bar chart
- **Prior/Posterior 비교**: mean ± std 비교 plot
- **active_dims, total KL 수치 표시**

---

## 저장 주기

| 모드 | 주기 | 시나리오 선택 |
|------|------|-------------|
| train | 200 step마다 | 배치 내 랜덤 1개 |
| val | val 할 때마다 | 배치 내 랜덤 1개 |
| test | 매 배치 | 매번 |

---

## 파일명 규칙

```
{out}/debug_distribution/
    train/
        step{global_step}_intent.png
        step{global_step}_map_attn.png
        step{global_step}_z_kl.png
    val/
        epoch{epoch}_b{batch_idx}_intent.png
        epoch{epoch}_b{batch_idx}_map_attn.png
        epoch{epoch}_b{batch_idx}_z_kl.png
    test/
        b{batch_idx}_intent.png
        b{batch_idx}_map_attn.png
        b{batch_idx}_z_kl.png
```

---

## 구현 순서

### 1. 기존 viz 코드 분석
- [ ] 맵 렌더링 방식 (map_env, crop, 좌표 변환)
- [ ] GT/pred trajectory 추출 및 denormalize 방식
- [ ] 기존 viz_scene_graph 등 참고

### 2. `src/utils/debug_viz_distribution.py` 생성
- [ ] `viz_intent(model, scene_graph, pred, loss_fn, save_path, step_idx)`
  - Intent 3×3 heatmap (model vs GT) + 맵 + 경로 + 현재 step 표시
- [ ] `viz_map_attn(model, scene_graph, pred, map_env, save_path, step_idx)`
  - Map attn 29×29 heatmap (model vs GT) + 맵 overlay + 경로
- [ ] `viz_z_kl(model, pred, save_path)`
  - z_global per-dim KL bar chart + prior/posterior 비교
- [ ] `save_debug_distribution(model, scene_graph, pred, loss_fn, map_env, save_dir, step_idx=None, ...)`
  - 위 3개를 한번에 호출하는 wrapper

### 3. Config parser 추가
- [ ] `debug_distribution: True/False` (기본 False)
- [ ] train_trafficplanner.py parser에 추가

### 4. train.py 연동
- [ ] `{out}/debug_distribution/{train,val}` 폴더 생성
- [ ] `run_one_epoch(train=True)`: global_step % 200 == 0 일 때 호출
- [ ] `run_one_epoch(train=False)`: 랜덤 배치에서 호출
- [ ] 배치 내 랜덤 시나리오 선택 (batch_idx 랜덤)

### 5. test 스크립트 연동
- [ ] `{out}/debug_distribution/test` 폴더 생성
- [ ] 매 배치마다 호출
- [ ] test_trafficplanner.py에 debug_distribution 옵션 추가

---

## 참고: 데이터 접근 경로

| 필요 데이터 | 접근 방법 |
|------------|----------|
| Intent weights (model) | `model.get_intent_weights()` → (FT, N_ego, 9) |
| Intent GT soft label | loss.py의 로직 재활용 (acc/yaw → 9 prototype 거리) |
| Map attn weights (model) | `model.get_ego_map_attn_weights()` → reshape to 29×29 |
| Map attn GT soft label | `loss_fn._make_soft_label()` 재활용 |
| z_global prior/posterior | `model._last_prior_out`, `model._last_posterior_out` |
| GT trajectory | `scene_graph.future` (denormalize 필요) |
| Pred trajectory | `pred['future_pred']` (denormalize 필요) |
| Map image | `map_env` 객체에서 crop |

## 핵심 주의사항

- **TF mode에서 intent/attn은 shifted**: `[PT-1:-1]` 슬라이싱 적용된 상태
- **좌표계 일치**: map_recrop=True면 각 step 기준 crop, attn은 해당 crop 기준
- **코드 주석 영어로**
- **matplotlib Agg backend** 사용 (headless 환경)
