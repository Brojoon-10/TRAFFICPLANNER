# TODO 0308 — 근본적 구조 개선

## 현재 상태 진단

**핵심 문제: decoder가 z, map, intent 없이도 past heading 외삽만으로 pos_err 0.95m 달성**
- z_global: 32차원 중 30개 collapse (dim 7, 27만 생존)
- map: map_attn_loss=0 → attention이 drivable area에 집중 안 함 → 도로 밖 trajectory 허용
- intent: decoder가 heading 외삽만 하니 acc/yaw_rate 구분 불필요 → uniform (CE ≈ 1.0)
- 샘플링: z~N(0,1) = 순수 노이즈 → 도로 무시한 랜덤 trajectory 생성

**증상 (시각화 확인)**:
- T자 교차로에서 좌회전 불가능한 방향으로 샘플 생성
- Y자 교차로에서 도로 없는 방향으로 heading 외삽
- "도로를 따라간다"가 아니라 "현재 heading을 유지한다"

---

## 구조 변경 (S1~S8)

### S1. ZCrossAttention 모듈 완전 제거
- **현재**: z_global(32) → ZCrossAttention(z_tokens(Q) × context_base(KV)) → z_context(4×128)
- **문제**: z_global은 32dim. 4토큰×128dim으로 expand해봐야 MLP 뻥튀기
- **삭제**: ZCrossAttention 모듈 전체, z_context 개념 제거
- **viz 영향**: `num_z_tokens` 파라미터 전달 제거 필요 (viz_attn_intent.py)

### S2. A2Z 제거 + z_global을 A2S query에 주입
- **현재 decoder layer**: A2A → A2Z(decoder×z_context) → A2S(decoder×map) → FFN
- **변경 decoder layer**: A2A → A2S(+z) → FFN
- **A2S query**: `query = W_q([decoder_hidden; z_proj(z_global)])` × map_tokens(KV)
- z_global이 "이 의도면 map에서 여기를 봐라" 직접 가이드
- A2Z 관련 파라미터 전부 제거 (a2z_*_proj, a2z_norm 등)
- **viz 영향**: map_attn_weights는 A2S 내부에서 저장 → 그대로 작동

### S3. z_gen residual 제거
- **현재**: `z_pooled = LN(z_query + o_proj(attn_out))`
- **변경**: `z_pooled = LN(o_proj(attn_out))`
- z_query 상수 바닥이 attn_out 무시 유발 → collapse 원인

### S4. num_z_queries 확대: 2 → 4
- context에서 더 다양한 정보 추출, z_size=32 유지
- 4×128 = 512 → MLP → 64 (z_size×2)

### S5. Intent를 마지막 layer 이후로 이동
- **현재**: layer 0 output → IntentCodebook → z_local → concat → layer 1 입력
- **변경**: 마지막 layer output → IntentCodebook → z_local → AdaLN으로 output_head 변조
- 2층 거친 hidden으로 acc/yaw_rate 판단 = 더 정확
- **viz 영향**: intent_weights/z_local getter는 동일, 저장 위치만 변경

### S6. output_head 구조 변경 — AdaLN + 직접 연결
- **현재**: `output_head(hidden)` — hidden(128)만
- **변경**: output_head 내부에 AdaLN 적용
  ```
  h = Linear(288 → 128)([hidden; last_cross_attn_out; z_global])
  h = LayerNorm(h)
  scale, shift = Linear(32 → 256)(z_local) → split
  h = h * (1 + scale) + shift    ← AdaLN: intent가 변조
  h = GELU(h)
  action = Linear(128 → action_dim)(h)
  ```
- **입력 분리**:
  - concat 입력 (288dim): hidden(128) + last_cross_attn_out(128) + z_global(32)
  - AdaLN 조건 (32dim): z_local — 곱하기로 변조, 구조적 무시 불가
- **왜 AdaLN**: concat은 MLP가 weight=0으로 무시 가능. 곱하기(scale)는 0 아닌 이상 영향

### S7. dist_aux_head — 마지막 layer cross_attn_out에만
- 마지막 layer A2S cross_attn_out → dist_aux_head → [solid_dist, dashed_dist] 예측
- layer 0 A2S는 supervision 없이 자유 학습

### S8. intent_ce weight 강화
- **현재**: 0.005 (recon에 묻힘)
- **변경**: 0.01~0.05 (z_local이 acc/yaw_rate를 정확히 담도록)
- AdaLN과 시너지: intent_ce가 z_local 정확도 보장, AdaLN이 구조적 사용 강제

---

## 최종 Decoder 구조

```
[매 AR step t]

layer 0:
  A2A(hidden × hidden, +rel_bias)                      — agent 상호작용
  A2S+z(W_q([hidden; z_proj(z_global)]) × map_tokens)  — 의도 담아 map 읽기
  FFN (ego/sur 분리)

layer 1:
  A2A(hidden × hidden, +rel_bias)
  A2S+z(W_q([hidden; z_proj(z_global)]) × map_tokens)  → last_cross_attn_out 저장
  FFN (ego/sur 분리)

post-decoder:
  IntentCodebook(layer1_ego_hidden) → z_local(32), intent_weights
  dist_aux_head(last_cross_attn_out) → dist_pred (학습 signal only)

  output_head:
    h = Linear([hidden(128); cross_attn(128); z_global(32)]) → 128  ← 288dim 입력
    h = LayerNorm(h)
    scale, shift = Linear(z_local(32)) → (128, 128)                 ← AdaLN 조건
    h = h * (1 + scale) + shift
    h = GELU(h)
    action = Linear(h) → action_dim
```

---

## Phase 1: Encoder 사전학습

**목표**: z_global이 future trajectory 정보를 인코딩 + CNN이 distance 정보를 토큰에 인코딩

**학습 대상**:
- context_encoder, prior, posterior, z_gen (z 경로)
- CNN map encoder, map_summary_pooling (context_base에 map 포함)
- GCN (past/future — context_base 입력)
- z_aux_head (MLP: 32→64→48)
- map_dist_head (MLP: token_dim→2)

**학습 안 함**: decoder layers, output_head, intent_codebook, dist_aux_head, AdaLN params

**Loss 1 — z_aux**: z_global(32) → MLP(32→64→48) → future (x, y, heading, speed) × 12 = 48 vs GT, MSE
- GT: normalized local frame 좌표 (x, y), heading = atan2(hy, hx), speed
- MLP 의도적으로 약하게 — z에 정보 압축 강제

**Loss 2 — map_dist_aux**: CNN 토큰별 [solid_dist, dashed_dist] 예측 vs GT distance field, MSE
- GT: distance field 이미지에서 각 token RF center 좌표 lookup
- decoder 불필요 — CNN 출력에 직접 supervision

**Loss 3 — KL**: 가벼운 정규화 (prior ≈ posterior)

**Epoch**: ~20-30 (수렴까지)

**Phase 1에서 불필요한 것들** (생성/초기화하지 않거나 forward에서 스킵):
- decoder layers (A2A, A2S, FFN)
- output_head (AdaLN 포함)
- intent_codebook, intent_ce_head
- dist_aux_head
- action blending, bicycle model

### Phase 1 구현 체크리스트
- [x] `trafficplanner_model.py`: z_aux_head = MLP(32→64→48) — (x,y,heading,speed)×12
- [x] `trafficplanner_model.py`: map_dist_head = MLP(token_dim→32→2)
- [x] `trafficplanner_model.py`: Phase 1 forward (decoder skip, dummy future_pred)
- [x] `trafficplanner_model.py`: freeze_for_phase1() 메서드
- [x] `trafficplanner_loss.py`: z_aux MSE loss (x,y,heading,speed × 12steps)
- [x] `trafficplanner_loss.py`: map_dist_aux MSE loss (token RF center에서 GT distance field lookup)
- [x] `train_trafficplanner.py`: Phase 1 freeze + trainable-only optimizer
- [x] `train_trafficplanner.py`: `--loss_map_dist_aux` weight 추가
- [x] `train_trafficplanner.py`: model.phase = 1/2 설정

---

## Phase 2: Frozen Encoder + Fresh Decoder

**목표**: decoder가 z_global + map + intent에 의존하여 trajectory 생성

**고정**: context_encoder, prior, posterior, z_gen, CNN, GCN, map_summary (Phase 1 결과)
**초기화 (random)**: decoder layers, output_head (AdaLN 포함), intent_codebook, dist_aux_head

**Loss**:
- recon (reconstruction)
- KL
- dist_aux (마지막 layer cross_attn_out → distance 예측, MSE)
- intent_ce (z_local → 3×3 soft label, KL div) — weight 0.01~0.05로 강하게
- potential_env (선택적)

### Phase 2 구현 체크리스트
- [x] Phase 1 checkpoint 로드 + encoder/CNN/GCN freeze (`freeze_z_global()` + context_encoder/map_summary 포함)
- [x] decoder/output_head/intent_codebook/dist_aux_head random reinit (`reinit_decoder()`)
- [x] `trafficplanner_model.py`: S2 — A2S query에 z_global concat+proj (a2s_z_proj + q_proj 2D→D)
- [x] `trafficplanner_model.py`: S5 — intent를 post-decoder로 이동 (layer loop 밖)
- [x] `trafficplanner_model.py`: S6 — output_head AdaLN (288dim입력, z_local scale/shift)
- [x] `trafficplanner_model.py`: S7 — dist_aux_head (128→64→2, last cross_attn_out)
- [x] `trafficplanner_loss.py`: dist_aux loss (ego center distance field MSE)
- [x] `train_trafficplanner.py`: `--loss_dist_aux` weight 추가
- [x] `train_trafficplanner.py`: Phase 2 reinit_decoder + optimizer (decoder params만)
- [x] `viz_attn_intent.py`: num_z_tokens→num_z_queries, 5ch map render (binary only)

---

## P4. Distance Field 채널 추가 (Phase 1 전에 구현)

**문제**: solid/dashed line이 1-pixel binary → CNN 다운샘플링에서 정보 소멸
**해결**: distance field 채널 추가

**채널 구성 (3ch → 5ch)**:
- ch0: drivable_area (binary)
- ch1: solid_line (binary)
- ch2: dashed_line (binary)
- ch3: solid_dist — `1/(1+d)`, d = 실선까지 거리(m)
- ch4: dashed_dist — `1/(1+d)`, d = 점선까지 거리(m)

### P4 구현 체크리스트
- [x] `fit_utils.py`: `cv2.distanceTransform` → `1/(1+d/pix_per_m)` 변환
- [x] `fit_map_env.py`: `num_layers=5`, ch3/ch4 추가
- [x] CNN 입력 자동 반영 확인 (`conv_channel_in=map_env.num_layers`)

---

## 보류 항목

### map_attn_loss → weight=0 유지
- dist_aux가 대체. 필요 시 재활성화

### Map Gating (P6) — 보류
- output_head에 cross_attn_out 직접 입력 + AdaLN이 구조적 대안
- 결과 보고 판단

### Context Dropout (P3) — 보류
- Phase 2에서 decoder가 z를 무시할 경우 대비
- S2 (A2S+z) + S6 (AdaLN)으로 충분할 수 있음

---

## 구현 순서

```
1. S1~S4: Encoder 측 구조 변경
   - ZCrossAttn 제거, A2Z 제거, z_gen residual 제거, K=4
2. P4: Distance field 채널 추가 (CNN 입력 5ch)
3. Phase 1 구현 + 학습 (~20-30 epoch)
   - z_aux_head, map_dist_head 추가
   - Phase 1 forward/loss/optimizer
4. S5~S8: Decoder 측 구조 변경
   - intent 이동 (post-decoder), A2S+z query, output_head AdaLN, dist_aux_head
5. Phase 2 구현 + 학습 (~50-80 epoch)
   - encoder freeze, decoder reinit, dist_aux/intent_ce loss
```

S1~S4는 encoder 측이라 Phase 1 전에.
S5~S8은 decoder 측이라 Phase 2 전에.

---

## viz 영향 정리

| 변경 | viz_attn_intent.py 영향 |
|------|------------------------|
| S1. ZCrossAttn 제거 | `num_z_tokens` 모델 인자 제거 필요 |
| S2. A2Z 제거 | 영향 없음 (viz에서 A2Z 미사용) |
| S2. A2S+z | map_attn_weights는 A2S 내부 저장 → 그대로 작동 |
| S5. Intent 위치 이동 | getter 동일 (`get_intent_weights()`, `get_z_local()`), 저장 위치만 변경 |
| S6. AdaLN | 영향 없음 |
| P4. 5ch map | `render_map_bg()` color/alpha list 확장 필요 (ch3,ch4 추가) |

---

## 예상 학습 파이프라인

```
Phase 1 (~20-30 epoch):
  python train_trafficplanner.py --phase 1 \
    --loss_z_aux 0.5 --loss_map_dist_aux 0.1 --loss_kl 0.001 \
    --loss_recon 0 --loss_map_attn 0 --loss_intent_ce 0 \
    --num_z_queries 4 \
    --out_dir out/phase1_encoder

Phase 2 (~50-80 epoch):
  python train_trafficplanner.py --phase 2 \
    --phase1_ckpt out/phase1_encoder/checkpoints/best_model.pth \
    --loss_recon 1.0 --loss_kl 0.004 \
    --loss_dist_aux 0.01 --loss_intent_ce 0.03 \
    --loss_map_attn 0 --loss_z_aux 0 --loss_map_dist_aux 0 \
    --num_z_queries 4 \
    --out_dir out/phase2_full
```
