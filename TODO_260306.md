# TODO — 260306: z_global 구조 개편

## 문제 진단

### 현상: z_global collapse (posterior collapse)
- KL loss: 1.15 (epoch 0) → 0.001 (epoch 124)
- z_mdist: 1.37 → 0.59 (prior ≈ posterior, z에 정보 없음)
- intent_ce: ~1.03 고정 (9-class 랜덤 수준)
- recon_loss: 3.79 → 3.75 (z 없이도 줄어듦)

### 근본 원인: 디코더가 z 없이도 예측 가능한 구조

현재 구조:
```
Context Encoder: past(4) + z_tokens(4) + map_summary(8) → self-attn → context(16, 128)
Decoder: A2A → A2C(context) → A2S(map) → FFN
```

**문제 1: z가 context에 희석됨**
- z_tokens(4)이 past(4) + map_summary(8)과 self-attention으로 섞임
- context 16토큰 전체에 z 정보가 분산 → 디코더 입장에서 z 기여분 구분 불가

**문제 2: 디코더가 past에 직접 접근 가능**
- A2C cross-attention으로 context(past+map 포함)를 직접 참조
- past trend (가속/감속/커브 패턴) 정보를 z 없이 얻을 수 있음
- → z를 무시해도 recon loss 감소 가능 → KL=0으로 수렴

**검증된 사실: past trend 없이 예측 불가능**
- 현재 state (속도, heading)만으로는 "감속 중인지 가속 중인지" 판별 불가
- 직진→커브 진입 vs 커브→직진 구분 불가
- 6초 후 예측에 past trend 정보 필수

### 핵심 통찰
> future 정보는 z 생성(posterior) 이후에는 z를 통해서만 전달되어야 한다.
> past trend도 z를 통해서만 접근 가능하게 만들면, z 의존이 "강제"가 아니라 "필연"이 된다.

---

## 해결: 구조 개편

### 기각된 대안들

| 대안 | 기각 이유 |
|------|----------|
| Gating (z로 context gate) | gate가 상수로 수렴 가능 → z = bias |
| z_aux loss 추가 | aux head가 독립 성장, main decoder는 여전히 z 무시 |
| KL annealing 변경 | 구조 문제를 loss 조절로 해결 불가 |
| z concat to decoder | MLP가 z weight를 0으로 학습 가능 |
| A2C 유지 + z 변조 | A2C로 past direct access 가능한 한 z 무시 경로 존재 |
| z를 key bias / score modulation | 비표준적, 검증 사례 적음 |

### 채택: z를 query로 context cross-attention + A2C 제거

**원리**: past trend 접근 경로를 z_context로 한정 → z가 의미 없으면 past trend 없이 예측 → recon loss 증가 → z 의존 필연

---

## 새 구조 상세

### 전체 흐름

```
[1회 실행 — Encoding]

(1) Past GCN: past 4 steps → past_tokens (4, 128)     ← temporal PE 부여
(2) Map CNN:  map image → map_tokens (57×57)
              map_pool → map_summary (8, 128)           ← PE 없음
(3) Context Encoder:
    [past_tokens + map_summary] → self-attn 2L → context (12, 128)
    ※ z 미포함 — past-map 관계만 정제 ("이 도로에서 감속" 등)

(4) CVAE:
    posterior(past, future, map) → z ~ N(μ, σ)         ← z 생성 (32d)
    prior(past, map) → z_prior                         ← KL 학습용

(5) z Cross-Attention:
    z_tokens = z_proj(z_global) → (4, 128)             ← z를 4개 토큰으로 분할
    z_context = cross_attn(Q=z_tokens, KV=context) → (4, 128)
    ※ z가 query → z가 "뭘 읽을지" 결정
    ※ z가 의미 없으면 엉뚱한 정보 읽음 → 예측 실패


[AR loop — Decoding, step t = 0..11]

(6) GCN(current_state) → gcn_feat (N_agents, 64)
(7) decoder_token = proj(gcn_feat ⊕ z_local ⊕ lw ⊕ sem) + step_PE(t)

(8) Decoder Layer ×2:
      A2A(decoder_token)                          → agent 상호작용
      A2Z(Q=위 결과, KV=z_context, 4토큰)         → z/past trend 정보 흡수
      A2S(Q=위 결과, KV=map_tokens_recrop)         → map 고해상도 정보
      FFN

    ※ Intent codebook: Layer 0 → 1 사이 삽입 (기존과 동일)

(9) output_head → action (a, ddh)
(10) blend: (1-α)*gt + α*pred
(11) bicycle_model → next_state
```

### 제거 항목

| 제거 대상 | 이유 |
|-----------|------|
| **A2C (context cross-attn)** | past direct access 차단 — z collapse 근본 원인 |
| **AdaLN** (이미 제거됨) | z가 context에 포함되지 않으므로 불필요 |
| Context encoder 내 z_tokens | z는 별도 cross-attention으로 분리 |

### 유지 항목

| 유지 대상 | 비고 |
|-----------|------|
| Context Encoder (past+map self-attn) | z만 빠짐, past-map 관계 정제 역할 유지 |
| CVAE (prior/posterior/KL) | z 생성 구조 동일 |
| GCN (encoder + decoder) | agent 상호작용 |
| Map CNN + A2S | 환경 정보 (고해상도) |
| A2A + RelBias | agent 상호작용 |
| Intent codebook + z_local | Layer 0→1 사이 |
| Bicycle model | action → state |
| Action blending | α schedule |
| 모든 loss 종류 | recon, KL, map_attn, intent_ce, sur/ego_pred, potential |

### 디코더 Layer 변경

```
현재:  A2A → A2C(context) → A2S(map) → FFN
변경:  A2A → A2Z(z_context) → A2S(map) → FFN
```

A2Z는 기존 A2C와 동일한 cross-attention 패턴 (ego/sur 분리 Q/O):
- Q: decoder token (agent별)
- K, V: z_context (4토큰, 모든 agent 공유)
- ego_q_proj / ego_o_proj, sur_q_proj / sur_o_proj 분리

### z Cross-Attention (신규 모듈)

```python
class ZCrossAttention(nn.Module):
    """z_tokens(Q) × context(KV) → z_context"""
    def __init__(self, d_model, nhead):
        self.q_proj = Linear(d_model, d_model)
        self.k_proj = Linear(d_model, d_model)
        self.v_proj = Linear(d_model, d_model)
        self.o_proj = Linear(d_model, d_model)
        self.norm = LayerNorm(d_model)

    def forward(self, z_tokens, context):
        # z_tokens: (B*NA, 4, D), context: (B*NA, 12, D)
        Q = self.q_proj(z_tokens)
        K = self.k_proj(context)
        V = self.v_proj(context)
        attn_out = multi_head_attn(Q, K, V)
        return z_tokens + self.o_proj(attn_out)  # residual
```

### 정보 경로 정리

| 정보 종류 | 경로 | z 의존 |
|-----------|------|--------|
| Past trend (가속/감속/커브) | posterior → z → z_context → A2Z | **필수** |
| Future 의도 (좌/우회전) | posterior → z → z_context → A2Z | **필수** |
| Map 환경 (도로/차선) | Map CNN → A2S | 불필요 |
| Agent 상호작용 | GCN → A2A | 불필요 |
| 현재 상태 (속도/heading) | GCN → decoder_token | 불필요 |

→ past trend + future 의도는 **z_context가 유일한 경로** → z 의존 필연

---

## 구현 계획

### 변경 파일

1. **trafficplanner_model.py** (주요 변경)
   - [ ] Context Encoder: z_tokens 입력 제거 (past+map만)
   - [ ] ZCrossAttention 클래스 추가
   - [ ] TransDecoderLayer: A2C 제거, A2Z 추가 (동일 패턴)
   - [ ] `__init__`: z cross-attention 모듈 생성, context encoder z 관련 제거
   - [ ] `transformer_decoder_training_blended()`: context→z_context 흐름 추가, A2C→A2Z 전환
   - [ ] `_run_ar_inference()`: 동일 변경

2. **train_trafficplanner.py**
   - [ ] config 파라미터 정리 (A2C 관련 제거)

3. **train_trafficplanner.cfg**
   - [ ] num_z_tokens: 4 유지
   - [ ] context encoder 관련 파라미터 정리

4. **test_trafficplanner.py**
   - [ ] AR inference 경로 업데이트

### 파라미터 수 변화 추정

| 모듈 | 현재 | 변경 후 | 차이 |
|------|------|---------|------|
| Context Encoder | ~300K | ~250K | -50K (z_tokens 입력 제거) |
| A2C (제거) | ~200K | 0 | -200K |
| Z Cross-Attn (추가) | 0 | ~130K | +130K |
| A2Z in decoder (추가) | 0 | ~200K | +200K |
| 기타 | ~2.5M | ~2.5M | 동일 |
| **합계** | **~3.0M** | **~3.1M** | **+80K** |

---

## 검증 방법

### 1차: Shape 테스트
- [ ] context encoder 출력: (B*NA, 12, 128) — z 미포함
- [ ] z_context: (B*NA, 4, 128)
- [ ] decoder A2Z 입출력 shape 확인

### 2차: 학습 모니터링 (핵심 지표)
- [ ] **KL > 0.5** 유지 (현재 0.001 → 목표 0.5+)
- [ ] **z_mdist > 1.5** (현재 0.59 → prior ≠ posterior 확인)
- [ ] **intent_ce 감소** (현재 1.03 고정 → 의미 있는 intent 학습)
- [ ] recon_loss 감소 속도 (z가 기여하면 더 빠르게 감소)

### 3차: 적대적 최적화
- [ ] z_global 변경 시 trajectory 변화: ||∂traj/∂z|| > 0
- [ ] 다른 z → 다른 경로 생성 확인

---

## 후속 과제: z 생성 방식 개편 + Prior/Posterior 통일

### 문제 진단

#### 현재 z 생성 구조
```
Prior:
  past_gcn(4步) → GRU(2L) → last_hidden(64d) → concat(past_h, map, sem) → MLP → μ,σ

Posterior:
  past_gcn(4步) → GRU(2L) → last_hidden(64d)   ← past 인코딩
  future_gcn(12步) → PE → Transformer(3L) → last_hidden(64d)  ← future 인코딩
  → concat(past_h, future_h, map, sem) → MLP(194d → 128 → 64d) → μ,σ
```

#### 문제점

| 문제 | 설명 |
|------|------|
| **정보 병목 (2중 압축)** | 시퀀스 → last_hidden(64d) 압축 → MLP 압축. 4step×6d를 64d 하나로 요약한 뒤 또 압축 |
| **선택적 읽기 불가** | MLP는 concat 벡터를 통째로 처리. "t=2 감속 정보"만 선택적으로 추출 불가 |
| **Prior/Posterior 비대칭** | Prior=GRU(2L), Posterior=Transformer(3L). 구조가 달라서 prior가 posterior를 모사하기 어려움 |
| **past/future 분리 인코딩** | past-future 간 직접적 상호작용 없이 각각 인코딩 후 concat |

#### 핵심 통찰
> MLP에 때려넣기 → cross-attention으로 선택적 읽기.
> learnable z_query가 "z에 뭘 담아야 하는지" 학습하면, 정보 추출이 목적에 맞게 최적화된다.

---

### 해결: Cross-Attention Pooling + Prior/Posterior 통일

#### 왜 이 구조가 좋은가

##### 1. z가 담아야 하는 정보의 본질

z_global은 "이 시나리오에서 ego가 앞으로 어떻게 움직일 것인가"를 요약해야 한다.
이를 위해 필요한 정보:

| 정보 종류 | 예시 | 출처 |
|-----------|------|------|
| **운동 의도** | "좌회전할 것이다", "감속→정지" | future trajectory |
| **운동 추세** | "현재 감속 중", "커브 진입 중" | past trajectory 시퀀스 |
| **환경 맥락** | "앞에 교차로", "커브 구간" | map |
| **시점별 변화** | "t=2에서 감속 시작, t=8에서 정지" | past+future temporal 패턴 |

이 정보들은 **시간축 상 흩어져 있고**, **서로 다른 종류**이며, **시나리오마다 중요한 부분이 다르다**.

##### 2. 기존 방식(MLP)이 이 정보를 못 담는 이유

```
concat(past_hidden_64d, future_hidden_64d, map_64d, sem_2d) → MLP → μ,σ
```

- **last_hidden 압축**: 4 step 시퀀스를 64d 하나로 요약 → "t=2에서 감속"이라는 시점 정보 소실
- **concat = 동등 취급**: past_hidden과 map_feat을 같은 가중치로 MLP에 통과 → "이 시나리오에서는 map이 더 중요" 같은 동적 판단 불가
- **고정 연산**: 모든 시나리오에서 동일한 MLP weight로 처리. 직진 시나리오와 좌회전 시나리오에서 같은 방식으로 z 추출

##### 3. Cross-Attention Pooling이 이를 해결하는 원리

```
z_query(Q) × [context_base + future_proj](KV) → z
```

**z_query = "z에 뭘 담을지 결정하는 학습된 질문"**

- z_query가 learnable parameter로 학습됨 → "z에 담기에 유용한 정보가 뭔지" 자체를 최적화
- 학습 초기: 랜덤 → 모든 KV에 균등 attend (≈ mean pooling)
- 학습 진행: recon loss gradient가 z_query를 업데이트 → "미래 예측에 필요한 정보를 잘 읽는 query"로 수렴
- 예: z_query head 0이 "운동 추세"에 특화, head 1이 "환경 맥락"에 특화 가능

**attention의 핵심 이점 — 동적 선택**:

- 직진 시나리오: z_query가 past의 속도 토큰에 높은 attention → "등속 직진" 정보 추출
- 좌회전 시나리오: z_query가 past의 heading 변화 + map의 교차로 토큰에 높은 attention → "커브+교차로" 정보 추출
- 같은 z_query weight지만, **KV 내용에 따라 다른 정보를 선택적으로 읽음**
- MLP는 이게 불가능 — 입력 위치별 weight가 고정

**시퀀스 전체 접근**:

- last_hidden 압축 없이 개별 time step 토큰을 직접 읽음
- "t=2 감속 시작" 정보가 t=2 토큰에 보존되어 있고, z_query가 이를 선택 가능
- temporal PE 덕분에 "이게 몇 번째 step인지"도 구분 가능

##### 4. Prior/Posterior 통일이 주는 구조적 이점

```
Prior:     z_query(Q) × context_base(KV=12)              → μ,σ
Posterior: z_query(Q) × [context_base + future](KV=24)    → μ,σ
```

- **같은 z_query, 같은 projection**: prior와 posterior가 동일한 "질문"을 동일한 방식으로 함
- **KV만 다름**: posterior는 future 토큰 12개가 추가됨 → future 정보가 z에 반영
- **prior가 posterior를 모사하기 쉬움**: 같은 attention 패턴에서 future KV만 빠진 것 → prior는 "future 없이도 비슷한 z를 추정"하도록 학습
- **기존 비대칭(GRU vs Transformer)**: 완전히 다른 연산 → prior가 posterior의 출력 분포를 흉내내기 어려움

---

#### 새 구조

```
[공통 — 1회 계산]
context_base = ContextEncoder([past_gcn(4)+PE, map_summary(8)]) → self-attn 2L → (12, 128)
  ※ 이미 존재하는 모듈. decoder용 + z 생성용으로 공유

future_proj = Linear(future_gcn(12步, 64d)) → (12, 128) + temporal_PE
  ※ raw projection + PE만. self-attention 없음
  ※ future 내부 관계는 z_query가 multi-head로 여러 토큰에 attend하며 파악

[Prior — inference 시 사용]
z_query(Q) × context_base(KV=12토큰) → cross-attn → MLP → μ,σ
  ※ past+map 정보에서 z에 담을 내용 선택적 추출

[Posterior — training 시 사용]
z_query(Q) × [context_base + future_proj](KV=24토큰) → cross-attn → MLP → μ,σ
  ※ KV에 future 토큰 추가 → future 정보도 z에 담을 수 있음
  ※ context_base는 동일하게 재사용 (추가 연산 없음)

[z_context 생성 — 이전 구조 개편에서 추가됨]
z_context = ZCrossAttn(z_tokens(Q) × context_base(KV=12토큰)) → (4, 128)
  ※ decoder에는 context_base만 노출 → future 정보 leak 없음
```

#### 설계 결정 및 근거

| 결정 | 이유 |
|------|------|
| **cross-attention pooling 채택** | 시퀀스 전체를 시점별로 접근 가능. 시나리오에 따라 다른 정보를 동적 선택. MLP의 고정 연산/last_hidden 압축 문제 해결 |
| **learnable z_query** | `nn.Parameter`로 "z에 뭘 담을지" 자체를 학습. recon loss가 역전파되며 "예측에 유용한 정보를 읽는 query"로 수렴 |
| **Prior/Posterior 동일 구조** | 같은 z_query, 같은 projection, KV만 다름. prior가 posterior를 모사하기 구조적으로 쉬움 |
| **KV 길이 달라도 OK** | cross-attention은 KV 길이에 제약 없음. Q 차원 같으면 출력 동일 |
| **future는 raw+PE (self-attn 없음)** | 16~24개 토큰이면 z_query cross-attn의 multi-head가 토큰 간 관계를 충분히 포착. 별도 encoder 불필요 |
| **future에 temporal PE 필수** | PE 없으면 z_query가 "0.5초 후 감속 vs 6초 후 감속" 구분 불가. 시점 정보는 z의 핵심 |
| **context_base 재사용** | decoder용으로 이미 계산됨. z 생성에서 KV로 공유하면 추가 연산 없음 |
| **Encoder 1개 유지** | Enc2(past+future self-attn) 추가 시 prior 구조 비대칭 발생. 우선 단순한 방식으로 검증 |

#### 기각된 대안

| 대안 | 기각 이유 |
|------|----------|
| Encoder 2개 (decoder용 + posterior용) | 파라미터 2배, prior/posterior 통일 목표에 역행 |
| past+future 함께 self-attn 후 z 생성 | decoder KV에 future가 섞이면 leak. 분리하려면 encoder 2개 필요 |
| future도 self-attn 후 concat | future 전용 encoder 필요, 복잡도 증가. 효과 불확실 |
| z_query 없이 기존 MLP 유지 | 정보 병목 + 고정 연산 문제 미해결 |
| 기존 비대칭 구조 유지 (GRU+Transformer) | prior가 posterior 모사 어려움. z 품질 하락 |

---

### 구현 상세

#### z_query 설계

```python
# learnable query — z에 담을 정보를 학습
self.z_query = nn.Parameter(torch.randn(num_z_queries, d_model))  # (K, 128)
# K=1~2. z_size=32d를 채우기에 충분. 많으면 과잉

# cross-attention projections (prior/posterior 공유)
self.z_gen_q_proj = nn.Linear(d_model, d_model)
self.z_gen_k_proj = nn.Linear(d_model, d_model)
self.z_gen_v_proj = nn.Linear(d_model, d_model)
self.z_gen_o_proj = nn.Linear(d_model, d_model)
self.z_gen_norm = nn.LayerNorm(d_model)

# cross-attn 출력 → μ,σ
self.z_gen_to_latent = MLP([num_z_queries * d_model, 128, z_size * 2])
```

#### future projection 설계

```python
# future_gcn(64d) → d_model(128d) + temporal PE
self.future_proj = nn.Linear(gcn_hidden_dim, d_model)  # 64 → 128
self.future_temporal_pe = nn.Embedding(FT, d_model)     # 12 steps → (12, 128)
```

#### forward 흐름 (z 생성 부분)

```python
def _generate_z(self, context_base, future_gcn_seq=None):
    """
    Cross-attention pooling으로 z_global 생성.

    Prior: future_gcn_seq=None → KV=context_base(12토큰)
    Posterior: future_gcn_seq 제공 → KV=context_base+future_proj(24토큰)
    """
    NA = context_base.size(0)

    # z_query broadcast: (K, D) → (NA, K, D)
    q = self.z_query.unsqueeze(0).expand(NA, -1, -1)

    # KV 구성
    if future_gcn_seq is not None:
        # Posterior: context + future
        future_tokens = self.future_proj(future_gcn_seq)  # (NA, 12, 128)
        t_ids = torch.arange(self.FT, device=context_base.device)
        future_tokens = future_tokens + self.future_temporal_pe(t_ids)
        kv = torch.cat([context_base, future_tokens], dim=1)  # (NA, 24, 128)
    else:
        # Prior: context only
        kv = context_base  # (NA, 12, 128)

    # Cross-attention: z_query(Q) × kv(KV)
    q_proj = self.z_gen_q_proj(q)
    k_proj = self.z_gen_k_proj(kv)
    v_proj = self.z_gen_v_proj(kv)
    attn_out = multi_head_attention(q_proj, k_proj, v_proj)
    z_pooled = self.z_gen_norm(q + self.z_gen_o_proj(attn_out))  # (NA, K, D)

    # flatten → MLP → μ,σ
    z_flat = z_pooled.reshape(NA, -1)  # (NA, K*D)
    z_params = self.z_gen_to_latent(z_flat)  # (NA, z_size*2)
    mean, logvar = z_params[:, :self.z_size], z_params[:, self.z_size:]
    var = torch.exp(logvar)
    return mean, var
```

---

### 제거 항목

| 제거 대상 | 이유 |
|-----------|------|
| `prior_temporal_gru` (GRU 2L) | cross-attention pooling이 대체 — context_base를 KV로 직접 읽음 |
| `transformer_encoder` (Transformer 3L) | 위와 동일 — future 시퀀스 인코딩 불필요 |
| `positional_encoding` (sinusoidal PE) | future용 temporal PE(`nn.Embedding`)로 대체 |
| `latent_prior_net` (MLP) | `z_gen_to_latent`로 통합 |
| `latent_posterior_net` (MLP) | 위와 동일 — prior/posterior 같은 MLP 사용 |
| `prior()` 메서드 | `_generate_z(context_base)` 호출로 대체 |
| `encoder()` 메서드 | `_generate_z(context_base, future_gcn_seq)` 호출로 대체 |
| `_run_temporal_encoder()` 내 GRU/Transformer 분기 | GCN per-step 결과를 직접 사용 |

### 유지 항목

| 유지 대상 | 비고 |
|-----------|------|
| `temporal_gcn_encoder` + `step_feature_extractor` | per-step GCN은 그대로. past_gcn_seq 생성용 |
| ContextEncoder (past+map self-attn) | context_base 생성. decoder용 + z생성 KV용 공유 |
| ZCrossAttention | z(Q) × context(KV) → z_context. 이전 구조 개편에서 추가 |
| CVAE 구조 (KL loss, rsample 등) | z 생성 방식만 변경, CVAE 프레임워크 동일 |
| 나머지 전부 | decoder, loss, bicycle model 등 |

---

### 변경 파일

1. **trafficplanner_model.py** (주요 변경)
   - [ ] `__init__`: z_query, z_gen_* projections, future_proj, future_temporal_pe 추가
   - [ ] `__init__`: prior_temporal_gru, transformer_encoder, positional_encoding, latent_prior/posterior_net 제거
   - [ ] `_generate_z()` 메서드 추가 (cross-attention pooling)
   - [ ] `prior()` → `_generate_z(context_base)` 호출로 교체
   - [ ] `encoder()` → `_generate_z(context_base, future_gcn_seq)` 호출로 교체
   - [ ] `forward()`: context_base 계산 후 z 생성 흐름 변경
   - [ ] `_run_temporal_encoder()`: past GCN 시퀀스 추출 부분만 유지, GRU/Transformer 분기 제거 또는 분리
   - [ ] `compute_health_metrics()`: prior/posterior 모니터링 유지
   - [ ] `compute_per_module_grad_norms()`: prior/posterior 그룹 업데이트

2. **train_trafficplanner.py**
   - [ ] z 생성 관련 config 파라미터 추가/정리 (num_z_queries 등)
   - [ ] 로그 출력 업데이트

3. **train_trafficplanner.cfg**
   - [ ] num_z_queries: 2 추가
   - [ ] 제거된 파라미터 정리

4. **test_forward_pass.py**
   - [ ] z 생성 관련 assertion 업데이트

5. **test_trafficplanner.py**
   - [ ] inference 경로 확인 (prior 사용)

---

### 파라미터 수 변화 추정

| 모듈 | 현재 | 변경 후 | 차이 |
|------|------|---------|------|
| prior_temporal_gru (GRU 2L, 64d) | ~50K | 0 | -50K |
| transformer_encoder (3L, 64d) | ~75K | 0 | -75K |
| latent_prior_net (MLP) | ~12K | 0 | -12K |
| latent_posterior_net (MLP) | ~25K | 0 | -25K |
| positional_encoding | ~1K | 0 | -1K |
| z_query (nn.Parameter) | 0 | ~0.3K | +0.3K |
| z_gen_* projections (4×Linear 128→128) | 0 | ~66K | +66K |
| z_gen_to_latent (MLP) | 0 | ~42K | +42K |
| future_proj (Linear 64→128) | 0 | ~8K | +8K |
| future_temporal_pe (Embedding 12×128) | 0 | ~1.5K | +1.5K |
| **합계** | **~163K** | **~118K** | **-45K** |

---

### 검증 방법

#### 1차: Shape 테스트
- [x] prior: z_query(NA,2,128) × context_base(NA,12,128) → μ,σ (NA, 32) 각각
- [x] posterior: z_query(NA,2,128) × KV(NA,24,128) → μ,σ (NA, 32) 각각
- [x] prior/posterior 출력 shape 동일 확인
- [x] 11개 forward pass 테스트 전체 통과

#### 2차: 학습 모니터링
- [ ] KL > 0.5 유지 (z에 의미 있는 정보 담기는지)
- [ ] z_mdist > 1.5 (prior ≠ posterior)
- [ ] intent_ce 감소 (z가 의도 정보 담는지)
- [ ] recon_loss 감소 속도 비교

#### 3차: 적대적 최적화
- [ ] z_global 변경 시 trajectory 변화 확인

---

## ZCrossAttention 설계 근거

### 역할
z_global(32d bottleneck) → z_tokens(Q) × context_base(KV) → z_context(4×128d)
디코더에 z + context 디테일을 결합하여 전달하는 **세미 디코더**.

### 왜 z를 그냥 넘기면 안 되는가 (MLP 확장 vs Cross-Attention)

| | MLP 확장 (z→proj→z_tokens) | ZCrossAttention |
|---|---|---|
| **연산** | 고정 선형 변환 | z 내용에 따라 context에서 동적 선택 |
| **표현력** | z가 뭐든 같은 방식으로 확장 | "좌회전 z"면 좌측 차선 attend, "가속 z"면 전방 공간 attend |
| **정보량** | 32d → 512d (MLP weight에 내재된 정보만) | 32d + context 12×128d에서 선택적 복원 |

**핵심**: z_global은 KL regularization에 의해 32d bottleneck으로 제한됨.
이 32d에는 "좌회전", "가속" 같은 **의도**만 남고,
"옆 차 3m 거리", "전방 곡률 0.05" 같은 **디테일은 버려짐**.
ZCrossAttention이 z의 의도에 맞춰 context에서 디테일을 **선택적으로 복원**함.

### 학술적 근거

#### 1. Information Bottleneck Theory (Tishby 2000, Alemi ICLR 2017)

VAE의 z는 information bottleneck:
- KL term = I(X;Z) 상한 → z에 담을 수 있는 정보량을 **이론적으로 제한**
- β-VAE (Higgins 2017, Burgess 2017)에서 β>1이면 z 용량이 더 축소됨
- 우리 모델: kl_free_bits=0.05, KL weight=0.004 → z 32d에 담기는 정보 제한적

**따라서**: z bottleneck 통과 후 원본(context)을 재참조해서 디테일을 복원하는 것은
information bottleneck 이론에서 자연스러운 해법.

참고:
- Alemi et al., "Deep Variational Information Bottleneck", ICLR 2017
- Burgess et al., "Understanding disentangling in β-VAE", 2017

#### 2. CVAE trajectory prediction의 context 재참조 패턴

Trajectory prediction CVAE 문헌에서 z + context 결합은 표준 패턴:
- **Trajectron++ (Salzmann et al., ECCV 2020)**: z + context를 concat해서 decoder에 전달
- **CVAE-H (Greer et al., 2022)**: hypernetwork로 condition 정보를 decoder 전 레이어에 주입
- 우리 ZCrossAttention은 concat/hypernetwork 대신 **attention 기반 선택적 참조**
  → concat보다 유연 (z 내용에 따라 다른 context 부분 활용)

#### 3. Content-dependent routing (attention의 본질적 이점)

MLP: 입력 위치별 weight 고정 → 모든 시나리오에서 동일한 변환
Cross-attention: Q 내용에 따라 KV에서 다른 부분 선택 → **동적 라우팅**

이는 Transformer attention의 근본 이점이며,
"왜 MLP 대신 attention을 쓰는가"에 대한 일반적 답변과 동일.

### 왜 인코더 위치에 있는가

z_global과 context_base는 12-step AR loop 동안 **불변**.
디코더 안에 넣으면 동일 연산을 12번 반복 → 비효율.
한 번 계산하고 결과(z_context)를 디코더에 전달하는 것이 효율적.
